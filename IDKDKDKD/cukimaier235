--[[
    Quantum Onyx Project UI Library (GUIENC)
    Accurately matching the Quantum Onyx Project aesthetic:
    - Sleek Onyx / Dark Slate theme with Vibrant ASUS ROG Cyber Red & Cyan accents
    - Topbar with Title, Subtitle (Game • Version • Day), Settings & Credits pill buttons, Resize, Close
    - Horizontal Tab Navigation Bar with integrated live Search bar and animated ASUS ROG neon red underline indicator
    - 2-Column Responsive Layout (Left & Right) with signature gradient pill section headers
    - Complete suite of components: Dropdowns (with selector badge and chevron), Sliders (with value box and progress track), Toggles (with animated pill & lock icon), Buttons, Paragraphs, Inputs, Keybinds
    - 100% Backward compatible with ShielDTeam scripts (BFSHIELDTEAM, NewFish5, etc.)
]]

local Players = game:GetService("Players")
local Player = Players.LocalPlayer
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local HttpService = game:GetService("HttpService")
local TextService = game:GetService("TextService")

local ProtectGui = protectgui or (syn and syn.protect_gui) or function() end

local Custom = {} do
    Custom.ColorRGB = Color3.fromRGB(255, 45, 75)       -- ASUS ROG Cyber Neon Red
    Custom.AccentBright = Color3.fromRGB(255, 45, 75)   -- Vibrant ROG Neon Red Accent
    Custom.AccentColor = Color3.fromRGB(210, 20, 50)     -- Deep ROG Crimson
    Custom.AccentGlow = Color3.fromRGB(255, 95, 120)    -- ROG Glow Red / Coral
    Custom.BackgroundDark = Color3.fromRGB(10, 13, 20)   -- Deep ROG Titanium Carbon Chassis
    Custom.CardDark = Color3.fromRGB(14, 18, 26)         -- Cyberpunk Metal Slate
    Custom.StrokeColor = Color3.fromRGB(34, 44, 62)      -- Dark Cyber Border
    Custom.StrokeCyan = Color3.fromRGB(0, 195, 255)    -- Cyber Cyan Border (for Dropdowns)
    Custom.StrokeRed = Color3.fromRGB(255, 45, 75)       -- Active ROG Red Border
    Custom.Cyan = Color3.fromRGB(0, 195, 255)            -- ROG Cyber Cyan Accent
    Custom.BorderColor = Color3.fromRGB(34, 44, 62)      -- Border color alias

    function Custom:Create(Name, Properties, Parent)
        local _instance = Instance.new(Name)
        for i, v in pairs(Properties) do
            _instance[i] = v
        end
        if Parent then
            _instance.Parent = Parent
        end
        return _instance
    end

    -- Precision ASUS ROG Chamfered Card Builder (45-degree Beveled Cut Corners)
    function Custom:CreateChamferCard(cfg)
        local parent = cfg.Parent
        local size = cfg.Size or UDim2.new(1, 0, 0, 36)
        local pos = cfg.Position or UDim2.new(0, 0, 0, 0)
        local anchor = cfg.AnchorPoint or Vector2.new(0, 0)
        local layoutOrder = cfg.LayoutOrder or 0
        local name = cfg.Name or "ChamferCard"
        local C = cfg.Chamfer or 6
        local strokeColor = cfg.StrokeColor or Custom.StrokeColor
        local fillColor = cfg.FillColor or Custom.CardDark
        local strokeThickness = cfg.StrokeThickness or 1
        local cornerThickness = cfg.CornerThickness or strokeThickness
        local cornerColor = cfg.CornerColor or cfg.CornerStrokeColor or strokeColor
        local cornerTransparency = cfg.CornerTransparency or 0
        local zIndex = cfg.ZIndex or 2
        local clips = cfg.ClipsDescendants or false

        -- Root container (transparent, holds fill, stroke, and card content)
        local root = Custom:Create("Frame", {
            BackgroundColor3 = fillColor or Custom.CardDark,
            BackgroundTransparency = 1,
            BorderSizePixel = 0,
            Size = size,
            Position = pos,
            AnchorPoint = anchor,
            LayoutOrder = layoutOrder,
            ClipsDescendants = clips,
            Name = name,
            ZIndex = zIndex
        }, parent)

        local fillFrames = {}
        local cornerSize = math.round(C * 1.41421356)

        -- 1. Center Horizontal Fill: spans full width from y=C to y=H-C
        local CenterH = Custom:Create("Frame", {
            BackgroundColor3 = fillColor,
            BorderSizePixel = 0,
            Position = UDim2.new(0, 0, 0, C),
            Size = UDim2.new(1, 0, 1, -2 * C),
            ZIndex = zIndex,
            Name = "CenterH"
        }, root)
        table.insert(fillFrames, CenterH)

        -- 2. Center Vertical Fill: spans width from x=C to x=W-C, height full
        local CenterV = Custom:Create("Frame", {
            BackgroundColor3 = fillColor,
            BorderSizePixel = 0,
            Position = UDim2.new(0, C, 0, 0),
            Size = UDim2.new(1, -2 * C, 1, 0),
            ZIndex = zIndex,
            Name = "CenterV"
        }, root)
        table.insert(fillFrames, CenterV)

        -- 3. Four Corner Triangles (Strictly clipped containers with rotated 45-degree squares)
        -- Top-Left Corner
        local TL_Box = Custom:Create("Frame", {
            BackgroundColor3 = fillColor or Custom.CardDark,
            BackgroundTransparency = 1,
            BorderSizePixel = 0,
            Position = UDim2.new(0, 0, 0, 0),
            Size = UDim2.new(0, C, 0, C),
            ClipsDescendants = true,
            ZIndex = zIndex,
            Name = "TL_Box"
        }, root)
        local TL_Fill = Custom:Create("Frame", {
            BackgroundColor3 = fillColor,
            BorderSizePixel = 0,
            AnchorPoint = Vector2.new(0.5, 0.5),
            Position = UDim2.new(1, 0, 1, 0),
            Size = UDim2.new(0, cornerSize, 0, cornerSize),
            Rotation = 45,
            ZIndex = zIndex,
            Name = "TL_Fill"
        }, TL_Box)
        table.insert(fillFrames, TL_Fill)

        -- Top-Right Corner
        local TR_Box = Custom:Create("Frame", {
            BackgroundColor3 = fillColor or Custom.CardDark,
            BackgroundTransparency = 1,
            BorderSizePixel = 0,
            Position = UDim2.new(1, -C, 0, 0),
            Size = UDim2.new(0, C, 0, C),
            ClipsDescendants = true,
            ZIndex = zIndex,
            Name = "TR_Box"
        }, root)
        local TR_Fill = Custom:Create("Frame", {
            BackgroundColor3 = fillColor,
            BorderSizePixel = 0,
            AnchorPoint = Vector2.new(0.5, 0.5),
            Position = UDim2.new(0, 0, 1, 0),
            Size = UDim2.new(0, cornerSize, 0, cornerSize),
            Rotation = 45,
            ZIndex = zIndex,
            Name = "TR_Fill"
        }, TR_Box)
        table.insert(fillFrames, TR_Fill)

        -- Bottom-Left Corner
        local BL_Box = Custom:Create("Frame", {
            BackgroundColor3 = fillColor or Custom.CardDark,
            BackgroundTransparency = 1,
            BorderSizePixel = 0,
            Position = UDim2.new(0, 0, 1, -C),
            Size = UDim2.new(0, C, 0, C),
            ClipsDescendants = true,
            ZIndex = zIndex,
            Name = "BL_Box"
        }, root)
        local BL_Fill = Custom:Create("Frame", {
            BackgroundColor3 = fillColor,
            BorderSizePixel = 0,
            AnchorPoint = Vector2.new(0.5, 0.5),
            Position = UDim2.new(1, 0, 0, 0),
            Size = UDim2.new(0, cornerSize, 0, cornerSize),
            Rotation = 45,
            ZIndex = zIndex,
            Name = "BL_Fill"
        }, BL_Box)
        table.insert(fillFrames, BL_Fill)

        -- Bottom-Right Corner
        local BR_Box = Custom:Create("Frame", {
            BackgroundColor3 = fillColor or Custom.CardDark,
            BackgroundTransparency = 1,
            BorderSizePixel = 0,
            Position = UDim2.new(1, -C, 1, -C),
            Size = UDim2.new(0, C, 0, C),
            ClipsDescendants = true,
            ZIndex = zIndex,
            Name = "BR_Box"
        }, root)
        local BR_Fill = Custom:Create("Frame", {
            BackgroundColor3 = fillColor,
            BorderSizePixel = 0,
            AnchorPoint = Vector2.new(0.5, 0.5),
            Position = UDim2.new(0, 0, 0, 0),
            Size = UDim2.new(0, cornerSize, 0, cornerSize),
            Rotation = 45,
            ZIndex = zIndex,
            Name = "BR_Fill"
        }, BR_Box)
        table.insert(fillFrames, BR_Fill)

        -- 4. Perimeter Stroke Segments (8 segments: 4 straight edges + 4 diagonal corner cuts in ROG Red)
        local strokeLines = {}
        local cornerLines = {}
        if strokeThickness and strokeThickness > 0 and strokeColor then
            local strokeZ = zIndex + 1
            local cornerZ = strokeZ + 1

            local TopLine = Custom:Create("Frame", {
                BackgroundColor3 = strokeColor,
                BorderSizePixel = 0,
                Position = UDim2.new(0, C, 0, 0),
                Size = UDim2.new(1, -2 * C, 0, strokeThickness),
                ZIndex = strokeZ,
                Name = "TopLine"
            }, root)
            table.insert(strokeLines, TopLine)

            local BottomLine = Custom:Create("Frame", {
                BackgroundColor3 = strokeColor,
                BorderSizePixel = 0,
                Position = UDim2.new(0, C, 1, -strokeThickness),
                Size = UDim2.new(1, -2 * C, 0, strokeThickness),
                ZIndex = strokeZ,
                Name = "BottomLine"
            }, root)
            table.insert(strokeLines, BottomLine)

            local LeftLine = Custom:Create("Frame", {
                BackgroundColor3 = strokeColor,
                BorderSizePixel = 0,
                Position = UDim2.new(0, 0, 0, C),
                Size = UDim2.new(0, strokeThickness, 1, -2 * C),
                ZIndex = strokeZ,
                Name = "LeftLine"
            }, root)
            table.insert(strokeLines, LeftLine)

            local RightLine = Custom:Create("Frame", {
                BackgroundColor3 = strokeColor,
                BorderSizePixel = 0,
                Position = UDim2.new(1, -strokeThickness, 0, C),
                Size = UDim2.new(0, strokeThickness, 1, -2 * C),
                ZIndex = strokeZ,
                Name = "RightLine"
            }, root)
            table.insert(strokeLines, RightLine)

            local diagLen = math.floor(C * 1.41421356)

            -- 4 High-Contrast ROG Corner Diagonal Cut Lines (Red by default so corners pop!)
            local TL_Diag = Custom:Create("Frame", {
                BackgroundColor3 = cornerColor,
                BackgroundTransparency = cornerTransparency,
                BorderSizePixel = 0,
                AnchorPoint = Vector2.new(0.5, 0.5),
                Position = UDim2.new(0, C / 2, 0, C / 2),
                Size = UDim2.new(0, diagLen, 0, cornerThickness),
                Rotation = -45,
                ZIndex = cornerZ,
                Name = "TL_Diag"
            }, root)
            table.insert(cornerLines, TL_Diag)

            local TR_Diag = Custom:Create("Frame", {
                BackgroundColor3 = cornerColor,
                BackgroundTransparency = cornerTransparency,
                BorderSizePixel = 0,
                AnchorPoint = Vector2.new(0.5, 0.5),
                Position = UDim2.new(1, -C / 2, 0, C / 2),
                Size = UDim2.new(0, diagLen, 0, cornerThickness),
                Rotation = 45,
                ZIndex = cornerZ,
                Name = "TR_Diag"
            }, root)
            table.insert(cornerLines, TR_Diag)

            local BL_Diag = Custom:Create("Frame", {
                BackgroundColor3 = cornerColor,
                BackgroundTransparency = cornerTransparency,
                BorderSizePixel = 0,
                AnchorPoint = Vector2.new(0.5, 0.5),
                Position = UDim2.new(0, C / 2, 1, -C / 2),
                Size = UDim2.new(0, diagLen, 0, cornerThickness),
                Rotation = 45,
                ZIndex = cornerZ,
                Name = "BL_Diag"
            }, root)
            table.insert(cornerLines, BL_Diag)

            local BR_Diag = Custom:Create("Frame", {
                BackgroundColor3 = cornerColor,
                BackgroundTransparency = cornerTransparency,
                BorderSizePixel = 0,
                AnchorPoint = Vector2.new(0.5, 0.5),
                Position = UDim2.new(1, -C / 2, 1, -C / 2),
                Size = UDim2.new(0, diagLen, 0, cornerThickness),
                Rotation = -45,
                ZIndex = cornerZ,
                Name = "BR_Diag"
            }, root)
            table.insert(cornerLines, BR_Diag)
        end

        local cardData = {
            Root = root,
            CenterH = CenterH,
            CenterV = CenterV,
            TL_Fill = TL_Fill,
            TR_Fill = TR_Fill,
            BL_Fill = BL_Fill,
            BR_Fill = BR_Fill,
            FillBase = CenterH,
            FillFrames = fillFrames,
            StrokeLines = strokeLines,
            CornerLines = cornerLines,
            CornerLinesHolder = root,
            SetFillColor = function(col, dur)
                for _, f in ipairs(fillFrames) do
                    if dur and dur > 0 then
                        TweenService:Create(f, TweenInfo.new(dur, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {BackgroundColor3 = col}):Play()
                    else
                        f.BackgroundColor3 = col
                    end
                end
            end,
            SetStrokeColor = function(col, dur)
                for _, s in ipairs(strokeLines) do
                    if dur and dur > 0 then
                        TweenService:Create(s, TweenInfo.new(dur, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {BackgroundColor3 = col}):Play()
                    else
                        s.BackgroundColor3 = col
                    end
                end
            end,
            SetCornerColor = function(col, dur)
                for _, s in ipairs(cornerLines) do
                    if dur and dur > 0 then
                        TweenService:Create(s, TweenInfo.new(dur, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {BackgroundColor3 = col}):Play()
                    else
                        s.BackgroundColor3 = col
                    end
                end
            end,
            SetFillTransparency = function(trans, dur)
                for _, f in ipairs(fillFrames) do
                    if dur and dur > 0 then
                        TweenService:Create(f, TweenInfo.new(dur, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {BackgroundTransparency = trans}):Play()
                    else
                        f.BackgroundTransparency = trans
                    end
                end
            end,
            SetStrokeTransparency = function(trans, dur)
                for _, s in ipairs(strokeLines) do
                    if dur and dur > 0 then
                        TweenService:Create(s, TweenInfo.new(dur, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {BackgroundTransparency = trans}):Play()
                    else
                        s.BackgroundTransparency = trans
                    end
                end
                for _, s in ipairs(cornerLines) do
                    if dur and dur > 0 then
                        TweenService:Create(s, TweenInfo.new(dur, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {BackgroundTransparency = trans}):Play()
                    else
                        s.BackgroundTransparency = trans
                    end
                end
            end
        }

        root:SetAttribute("ChamferCard", true)
        return root, cardData
    end

    -- Precision High-Performance Component Card with Stepped Cyber Chamfer Corners (Zero Rotation, 100% Scroll-Clipped, Zero Diamonds)
    function Custom:CreateComponentCard(cfg)
        local parent = cfg.Parent
        local size = cfg.Size or UDim2.new(1, 0, 0, 36)
        local pos = cfg.Position or UDim2.new(0, 0, 0, 0)
        local anchor = cfg.AnchorPoint or Vector2.new(0, 0)
        local layoutOrder = cfg.LayoutOrder or 0
        local name = cfg.Name or "ComponentCard"
        local C = cfg.CornerRadius or cfg.Chamfer or 6
        local strokeColor = cfg.StrokeColor or Custom.StrokeColor
        local fillColor = cfg.FillColor or Custom.CardDark
        local strokeThickness = cfg.StrokeThickness or 1
        local cornerColor = cfg.CornerColor or cfg.CornerStrokeColor or strokeColor
        local cornerTransparency = cfg.CornerTransparency or 0
        local strokeTransparency = cfg.StrokeTransparency or 0
        local zIndex = cfg.ZIndex or 2
        local clips = (cfg.ClipsDescendants ~= nil) and cfg.ClipsDescendants or false

        -- Root card container (strictly Rotation = 0 for 100% native hardware scissor clipping in ScrollingFrame)
        local root = Custom:Create("Frame", {
            BackgroundColor3 = fillColor,
            BackgroundTransparency = cfg.FillTransparency or 0,
            BorderSizePixel = 0,
            Size = size,
            Position = pos,
            AnchorPoint = anchor,
            LayoutOrder = layoutOrder,
            ClipsDescendants = clips,
            Name = name,
            ZIndex = zIndex
        }, parent)

        -- Smooth corner background matching chamfer radius
        local O = cfg.Protrusion or (C >= 5 and 2 or 1)
        local corner = nil
        if C and C > 0 then
            local cornerR = math.max(0, C - O)
            corner = Custom:Create("UICorner", {
                CornerRadius = UDim.new(0, cornerR),
                Name = "CardCorner"
            }, root)
        end

        local strokeLines = {}
        local cornerLines = {}

        if strokeThickness and strokeThickness > 0 and strokeColor then
            local strokeZ = zIndex + 1

            if C and C >= 2 then
                -- 1. Four Straight Perimeter Edge Segments (Between Chamfers)
                local TopLine = Custom:Create("Frame", {
                    BackgroundColor3 = strokeColor,
                    BackgroundTransparency = strokeTransparency,
                    BorderSizePixel = 0,
                    Position = UDim2.new(0, C, 0, 0),
                    Size = UDim2.new(1, -2 * C, 0, strokeThickness),
                    ZIndex = strokeZ,
                    Name = "TopLine"
                }, root)
                table.insert(strokeLines, TopLine)

                local BottomLine = Custom:Create("Frame", {
                    BackgroundColor3 = strokeColor,
                    BackgroundTransparency = strokeTransparency,
                    BorderSizePixel = 0,
                    Position = UDim2.new(0, C, 1, -strokeThickness),
                    Size = UDim2.new(1, -2 * C, 0, strokeThickness),
                    ZIndex = strokeZ,
                    Name = "BottomLine"
                }, root)
                table.insert(strokeLines, BottomLine)

                local LeftLine = Custom:Create("Frame", {
                    BackgroundColor3 = strokeColor,
                    BackgroundTransparency = strokeTransparency,
                    BorderSizePixel = 0,
                    Position = UDim2.new(0, 0, 0, C),
                    Size = UDim2.new(0, strokeThickness, 1, -2 * C),
                    ZIndex = strokeZ,
                    Name = "LeftLine"
                }, root)
                table.insert(strokeLines, LeftLine)

                local RightLine = Custom:Create("Frame", {
                    BackgroundColor3 = strokeColor,
                    BackgroundTransparency = strokeTransparency,
                    BorderSizePixel = 0,
                    Position = UDim2.new(1, -strokeThickness, 0, C),
                    Size = UDim2.new(0, strokeThickness, 1, -2 * C),
                    ZIndex = strokeZ,
                    Name = "RightLine"
                }, root)
                table.insert(strokeLines, RightLine)

                -- 2. Stepped 45-Degree Chamfer Corner Segments with Sharp Outward Protrusion
                local cornerHolder = Custom:Create("Frame", {
                    BackgroundTransparency = 1,
                    BorderSizePixel = 0,
                    Size = UDim2.new(1, 0, 1, 0),
                    ZIndex = strokeZ,
                    Name = "CornerLinesHolder"
                }, root)

                for i = -O, (C - 1 + O) do
                    -- Top-Left Corner Diagonal (protruding outward)
                    local tl = Custom:Create("Frame", {
                        BackgroundColor3 = cornerColor,
                        BackgroundTransparency = cornerTransparency,
                        BorderSizePixel = 0,
                        Position = UDim2.new(0, i, 0, C - 1 - i),
                        Size = UDim2.new(0, 1, 0, strokeThickness),
                        ZIndex = strokeZ,
                        Name = "TL_Step"
                    }, cornerHolder)
                    table.insert(cornerLines, tl)

                    -- Top-Right Corner Diagonal (protruding outward)
                    local tr = Custom:Create("Frame", {
                        BackgroundColor3 = cornerColor,
                        BackgroundTransparency = cornerTransparency,
                        BorderSizePixel = 0,
                        Position = UDim2.new(1, -C + i, 0, i),
                        Size = UDim2.new(0, 1, 0, strokeThickness),
                        ZIndex = strokeZ,
                        Name = "TR_Step"
                    }, cornerHolder)
                    table.insert(cornerLines, tr)

                    -- Bottom-Left Corner Diagonal (protruding outward)
                    local bl = Custom:Create("Frame", {
                        BackgroundColor3 = cornerColor,
                        BackgroundTransparency = cornerTransparency,
                        BorderSizePixel = 0,
                        Position = UDim2.new(0, i, 1, -C + i),
                        Size = UDim2.new(0, 1, 0, strokeThickness),
                        ZIndex = strokeZ,
                        Name = "BL_Step"
                    }, cornerHolder)
                    table.insert(cornerLines, bl)

                    -- Bottom-Right Corner Diagonal (protruding outward)
                    local br = Custom:Create("Frame", {
                        BackgroundColor3 = cornerColor,
                        BackgroundTransparency = cornerTransparency,
                        BorderSizePixel = 0,
                        Position = UDim2.new(1, -C + i, 1, -1 - i),
                        Size = UDim2.new(0, 1, 0, strokeThickness),
                        ZIndex = strokeZ,
                        Name = "BR_Step"
                    }, cornerHolder)
                    table.insert(cornerLines, br)
                end
            else
                local fullStroke = Custom:Create("UIStroke", {
                    Color = strokeColor,
                    Thickness = strokeThickness,
                    Transparency = strokeTransparency,
                    ApplyStrokeMode = Enum.ApplyStrokeMode.Border,
                    Name = "CardStroke"
                }, root)
                table.insert(strokeLines, fullStroke)
            end
        end

        local cardData = {
            Root = root,
            Corner = corner,
            StrokeLines = strokeLines,
            CornerLines = cornerLines,
            CornerLinesHolder = root:FindFirstChild("CornerLinesHolder"),
            SetFillColor = function(col, dur)
                if dur and dur > 0 then
                    TweenService:Create(root, TweenInfo.new(dur, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {BackgroundColor3 = col}):Play()
                else
                    root.BackgroundColor3 = col
                end
            end,
            SetStrokeColor = function(col, dur)
                for _, s in ipairs(strokeLines) do
                    if s:IsA("UIStroke") then
                        if dur and dur > 0 then
                            TweenService:Create(s, TweenInfo.new(dur, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {Color = col}):Play()
                        else
                            s.Color = col
                        end
                    else
                        if dur and dur > 0 then
                            TweenService:Create(s, TweenInfo.new(dur, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {BackgroundColor3 = col}):Play()
                        else
                            s.BackgroundColor3 = col
                        end
                    end
                end
                for _, c in ipairs(cornerLines) do
                    if dur and dur > 0 then
                        TweenService:Create(c, TweenInfo.new(dur, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {BackgroundColor3 = col}):Play()
                    else
                        c.BackgroundColor3 = col
                    end
                end
            end,
            SetCornerColor = function(col, dur)
                for _, c in ipairs(cornerLines) do
                    if dur and dur > 0 then
                        TweenService:Create(c, TweenInfo.new(dur, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {BackgroundColor3 = col}):Play()
                    else
                        c.BackgroundColor3 = col
                    end
                end
            end,
            SetFillTransparency = function(trans, dur)
                if dur and dur > 0 then
                    TweenService:Create(root, TweenInfo.new(dur, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {BackgroundTransparency = trans}):Play()
                else
                    root.BackgroundTransparency = trans
                end
            end,
            SetStrokeTransparency = function(trans, dur)
                for _, s in ipairs(strokeLines) do
                    if s:IsA("UIStroke") then
                        if dur and dur > 0 then
                            TweenService:Create(s, TweenInfo.new(dur, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {Transparency = trans}):Play()
                        else
                            s.Transparency = trans
                        end
                    else
                        if dur and dur > 0 then
                            TweenService:Create(s, TweenInfo.new(dur, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {BackgroundTransparency = trans}):Play()
                        else
                            s.BackgroundTransparency = trans
                        end
                    end
                end
                for _, c in ipairs(cornerLines) do
                    if dur and dur > 0 then
                        TweenService:Create(c, TweenInfo.new(dur, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {BackgroundTransparency = trans}):Play()
                    else
                        c.BackgroundTransparency = trans
                    end
                end
            end
        }

        root:SetAttribute("ComponentCard", true)
        return root, cardData
    end

    -- ═════════════════════════════════════════════════════════════
    -- QUANTUM ONYX / ROG CYBER THEMED IN-GAME HUD RESKIN
    -- Reskins TopBar Unibar, Action Buttons (Emote/Shiftlock/Camera), and Hotbar Slots
    -- ═════════════════════════════════════════════════════════════
    function Custom:ApplyGameHudTheme(cfg)
        cfg = cfg or {}
        local strokeColor = cfg.StrokeColor or Custom.StrokeColor or Color3.fromRGB(34, 44, 62)
        local cyanColor = cfg.CyanColor or Custom.Cyan or Color3.fromRGB(0, 195, 255)
        local redColor = cfg.RedColor or Custom.ColorRGB or Color3.fromRGB(255, 45, 75)
        local darkBg = cfg.DarkBg or Custom.CardDark or Color3.fromRGB(14, 18, 26)

        -- Universal Cyber Corner Attachment (uses 0-rotation stepped frames for 100% native scissor clipping)
        local function AttachCyberCorners(frame, chamfer, col, protrude)
            if not frame or not frame:IsA("GuiObject") then return end
            -- STRICT GUARD: Never attach to whole-screen or oversized containers
            local sz = frame.AbsoluteSize
            if sz.X > 350 or sz.Y > 120 then return end

            local old = frame:FindFirstChild("CyberHudCorners")
            if old then old:Destroy() end

            local C = chamfer or 3
            local O = protrude or 1
            local cCol = col or cyanColor

            local holder = Instance.new("Frame")
            holder.Name = "CyberHudCorners"
            holder.BackgroundTransparency = 1
            holder.BorderSizePixel = 0
            holder.Size = UDim2.new(1, 0, 1, 0)
            holder.ZIndex = math.max(frame.ZIndex + 1, 10)
            holder.Parent = frame

            local function makeLine(name, pos, sz, bgCol)
                local l = Instance.new("Frame")
                l.Name = name
                l.BackgroundColor3 = bgCol or strokeColor
                l.BorderSizePixel = 0
                l.Position = pos
                l.Size = sz
                l.ZIndex = holder.ZIndex
                l.Parent = holder
                return l
            end

            makeLine("TopLine", UDim2.new(0, C, 0, 0), UDim2.new(1, -2 * C, 0, 1), strokeColor)
            makeLine("BottomLine", UDim2.new(0, C, 1, -1), UDim2.new(1, -2 * C, 0, 1), strokeColor)
            makeLine("LeftLine", UDim2.new(0, 0, 0, C), UDim2.new(0, 1, 1, -2 * C), strokeColor)
            makeLine("RightLine", UDim2.new(1, -1, 0, C), UDim2.new(0, 1, 1, -2 * C), strokeColor)

            for i = -O, (C - 1 + O) do
                local tl = Instance.new("Frame")
                tl.BackgroundColor3 = cCol
                tl.BorderSizePixel = 0
                tl.Position = UDim2.new(0, i, 0, C - 1 - i)
                tl.Size = UDim2.new(0, 1, 0, 1)
                tl.ZIndex = holder.ZIndex + 1
                tl.Parent = holder

                local tr = Instance.new("Frame")
                tr.BackgroundColor3 = cCol
                tr.BorderSizePixel = 0
                tr.Position = UDim2.new(1, -C + i, 0, i)
                tr.Size = UDim2.new(0, 1, 0, 1)
                tr.ZIndex = holder.ZIndex + 1
                tr.Parent = holder

                local bl = Instance.new("Frame")
                bl.BackgroundColor3 = cCol
                bl.BorderSizePixel = 0
                bl.Position = UDim2.new(0, i, 1, -C + i)
                bl.Size = UDim2.new(0, 1, 0, 1)
                bl.ZIndex = holder.ZIndex + 1
                bl.Parent = holder

                local br = Instance.new("Frame")
                br.BackgroundColor3 = cCol
                br.BorderSizePixel = 0
                br.Position = UDim2.new(1, -C + i, 1, -1 - i)
                br.Size = UDim2.new(0, 1, 0, 1)
                br.ZIndex = holder.ZIndex + 1
                br.Parent = holder
            end
        end

        task.spawn(function()
            local CoreGui = game:GetService("CoreGui")
            local playerGui = Player:WaitForChild("PlayerGui", 10)
            if not playerGui then return end

            local guis = { playerGui }
            pcall(function() table.insert(guis, CoreGui) end)
            pcall(function() if gethui then table.insert(guis, gethui()) end end)

            -- CLEANUP: Remove any stray CyberHudCorners on oversized containers
            for _, g in ipairs(guis) do
                pcall(function()
                    for _, desc in ipairs(g:GetDescendants()) do
                        if desc.Name == "CyberHudCorners" and desc.Parent and desc.Parent:IsA("GuiObject") then
                            local sz = desc.Parent.AbsoluteSize
                            if sz.X > 350 or sz.Y > 120 then
                                desc:Destroy()
                            end
                        end
                    end
                end)
            end

            -- 1. THEME HOTBAR / BACKPACK SLOTS (Clean Cyan & Dark Onyx - ZERO RED!)
            local function SkinHotbarSlot(slot)
                if not slot or not slot:IsA("GuiObject") then return end

                pcall(function()
                    slot.ClipsDescendants = false
                    slot.BackgroundColor3 = darkBg
                    slot.BackgroundTransparency = 0.12
                    slot.BorderSizePixel = 0

                    local corner = slot:FindFirstChildOfClass("UICorner")
                    if corner then
                        corner.CornerRadius = UDim.new(0, 3)
                    else
                        local c = Instance.new("UICorner")
                        c.CornerRadius = UDim.new(0, 3)
                        c.Parent = slot
                    end

                    local strk = slot:FindFirstChildOfClass("UIStroke")
                    if strk then
                        strk.Color = strokeColor
                        strk.Thickness = 1
                    end

                    AttachCyberCorners(slot, 3, cyanColor, 1)

                    -- Style child frames and buttons inside slot
                    for _, ch in ipairs(slot:GetChildren()) do
                        if ch:IsA("GuiObject") and ch.Name ~= "CyberHudCorners" then
                            if ch:IsA("Frame") or ch:IsA("ImageButton") or ch:IsA("TextButton") then
                                pcall(function()
                                    if ch.BackgroundTransparency < 0.9 then
                                        ch.BackgroundColor3 = darkBg
                                    end
                                    local chC = ch:FindFirstChildOfClass("UICorner")
                                    if chC then chC.CornerRadius = UDim.new(0, 3) end
                                end)
                            end
                        end
                    end

                    -- Style text labels inside slot (Clean Cyan & Crisp White - ZERO RED)
                    for _, d in ipairs(slot:GetDescendants()) do
                        if d:IsA("TextLabel") then
                            local txt = d.Text
                            local n = d.Name:lower()
                            if tonumber(txt) or n:find("num") or n:find("key") or n:find("index") or n:find("slot") then
                                d.Font = Enum.Font.GothamBold
                                d.TextColor3 = cyanColor
                                d.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
                                d.TextStrokeTransparency = 0.3
                            elseif txt:find("^x%d+") or n:find("amount") or n:find("count") or n:find("quant") then
                                -- Quantities: Crisp White (standard clean Fisch look, NOT red)
                                d.Font = Enum.Font.GothamBold
                                d.TextColor3 = Color3.fromRGB(255, 255, 255)
                                d.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
                                d.TextStrokeTransparency = 0.3
                            elseif n:find("sub") or txt:lower():find("unrevealed") then
                                d.Font = Enum.Font.GothamMedium
                                d.TextColor3 = Color3.fromRGB(160, 180, 205)
                                d.TextStrokeTransparency = 0.5
                            elseif n:find("name") or n:find("title") or #txt > 0 then
                                d.Font = Enum.Font.GothamBold
                                d.TextColor3 = Color3.fromRGB(245, 248, 255)
                                d.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
                                d.TextStrokeTransparency = 0.4
                            end
                        end
                    end
                end)
            end

            local function ScanHotbar()
                pcall(function()
                    local hud = playerGui:FindFirstChild("hud")
                    local safezone = hud and hud:FindFirstChild("safezone")
                    local backpack = (safezone and safezone:FindFirstChild("backpack")) or playerGui:FindFirstChild("backpack")
                    local hotbar = backpack and backpack:FindFirstChild("hotbar")

                    if not hotbar then
                        local rGui = CoreGui:FindFirstChild("RobloxGui")
                        local bp = rGui and rGui:FindFirstChild("Backpack")
                        hotbar = bp and bp:FindFirstChild("Hotbar")
                    end

                    if hotbar then
                        for _, slot in ipairs(hotbar:GetChildren()) do
                            if slot:IsA("GuiObject") then
                                SkinHotbarSlot(slot)
                            end
                        end
                        if not hotbar:GetAttribute("CyberHooked") then
                            hotbar:SetAttribute("CyberHooked", true)
                            hotbar.ChildAdded:Connect(function(child)
                                task.wait(0.05)
                                if child:IsA("GuiObject") then
                                    SkinHotbarSlot(child)
                                end
                            end)
                            hotbar.DescendantAdded:Connect(function(desc)
                                if desc:IsA("TextLabel") then
                                    task.wait(0.05)
                                    local p = desc:FindFirstAncestorOfClass("Frame") or desc:FindFirstAncestorOfClass("ImageButton")
                                    if p and p.Parent == hotbar then
                                        SkinHotbarSlot(p)
                                    end
                                end
                            end)
                        end

                        -- Prompt text "Press (G) To Open" - Pure White with black outline (NO RED!)
                        if backpack then
                            for _, d in ipairs(backpack:GetChildren()) do
                                if d:IsA("TextLabel") and d.Text:lower():find("press") and d.Text:lower():find("open") then
                                    d.Font = Enum.Font.GothamBold
                                    d.TextColor3 = Color3.fromRGB(255, 255, 255)
                                    d.TextStrokeTransparency = 0.3
                                    d.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
                                end
                            end
                        end
                    end
                end)
            end

            -- Helper: Detect if element is inside Unibar Pill
            local function IsInsidePill(obj)
                local p = obj and obj.Parent
                for _ = 1, 6 do
                    if not p or p == CoreGui or p == playerGui then break end
                    local pw = p.AbsoluteSize.X
                    local pox = p.Size.X.Offset
                    local effPW = (pw > 0 and pw) or pox
                    if p:IsA("GuiObject") and effPW > 65 and (p:FindFirstChildOfClass("UICorner") or p.Name:lower():find("pill") or p.Name:lower():find("unibar")) then
                        return true
                    end
                    p = p.Parent
                end
                return false
            end

            -- Helper: Setup Interactive Hover for Icons & Frames (Frame Drop on Idle, Bright Logo on Hover)
            local function SetupInteractiveHover(triggerBtn, iconTargets, frameTarget, isStandalone)
                if not triggerBtn or not triggerBtn:IsA("GuiObject") then return end
                if triggerBtn:GetAttribute("CyberHoverHooked") then return end
                triggerBtn:SetAttribute("CyberHoverHooked", true)

                local isHovered = false

                local function updateVisuals()
                    pcall(function()
                        if isHovered then
                            -- Hover State: Logo Terang Benderang (Bright White)
                            for _, item in ipairs(iconTargets) do
                                if item and item.Parent then
                                    if item:IsA("ImageLabel") or item:IsA("ImageButton") then
                                        item.ImageColor3 = Color3.fromRGB(255, 255, 255)
                                        item.ImageTransparency = 0
                                    elseif item:IsA("Frame") then
                                        item.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
                                        item.BackgroundTransparency = 0
                                    end
                                end
                            end
                            if frameTarget and frameTarget.Parent then
                                if not isStandalone then
                                    -- Inner pill button: hover highlight appears
                                    frameTarget.BackgroundColor3 = Color3.fromRGB(30, 44, 64)
                                    frameTarget.BackgroundTransparency = 0.35
                                else
                                    -- Standalone button: subtle brightened tint
                                    frameTarget.BackgroundColor3 = Color3.fromRGB(20, 26, 38)
                                    frameTarget.BackgroundTransparency = 0.08
                                end
                            end
                        else
                            -- Idle State: Frame Drop (transparan) & Logo Redup/Dim
                            for _, item in ipairs(iconTargets) do
                                if item and item.Parent then
                                    if item:IsA("ImageLabel") or item:IsA("ImageButton") then
                                        item.ImageColor3 = Color3.fromRGB(150, 165, 185)
                                        item.ImageTransparency = 0.4
                                    elseif item:IsA("Frame") then
                                        item.BackgroundColor3 = Color3.fromRGB(150, 165, 185)
                                        item.BackgroundTransparency = 0.4
                                    end
                                end
                            end
                            if frameTarget and frameTarget.Parent then
                                if not isStandalone then
                                    -- Inner pill button: Frame Drop (background hilang/transparan)
                                    frameTarget.BackgroundTransparency = 1
                                else
                                    frameTarget.BackgroundColor3 = darkBg
                                    frameTarget.BackgroundTransparency = 0.15
                                end
                            end
                        end
                    end)
                end

                -- Apply initial idle state
                updateVisuals()

                triggerBtn.MouseEnter:Connect(function()
                    isHovered = true
                    updateVisuals()
                end)

                triggerBtn.MouseLeave:Connect(function()
                    isHovered = false
                    updateVisuals()
                end)

                -- Mobile touch support
                triggerBtn.InputBegan:Connect(function(inp)
                    if inp.UserInputType == Enum.UserInputType.Touch or inp.UserInputType == Enum.UserInputType.MouseButton1 then
                        isHovered = true
                        updateVisuals()
                    end
                end)
                triggerBtn.InputEnded:Connect(function(inp)
                    if inp.UserInputType == Enum.UserInputType.Touch or inp.UserInputType == Enum.UserInputType.MouseButton1 then
                        task.delay(0.2, function()
                            isHovered = false
                            updateVisuals()
                        end)
                    end
                end)
            end

            -- 2. THEME ACTION BUTTONS: Standalone Buttons (Roblox Menu Button, Emote, Shiftlock, Camera)
            local function SkinActionBtn(btn, col)
                if not btn or not btn:IsA("GuiObject") then return end
                -- Never skin items inside the unibar pill as standalone action buttons
                if IsInsidePill(btn) then return end

                local sz = btn.AbsoluteSize
                local ox = btn.Size.X.Offset
                local effW = (sz.X > 0 and sz.X) or ox
                if effW > 95 then return end

                pcall(function()
                    btn.ClipsDescendants = false
                    btn.BackgroundColor3 = darkBg
                    btn.BackgroundTransparency = 0.15
                    btn.BorderSizePixel = 0

                    local corner = btn:FindFirstChildOfClass("UICorner")
                    if corner then
                        corner.CornerRadius = UDim.new(0, 6)
                        if not corner:GetAttribute("CyberLocked") then
                            corner:SetAttribute("CyberLocked", true)
                            corner:GetPropertyChangedSignal("CornerRadius"):Connect(function()
                                if corner.CornerRadius ~= UDim.new(0, 6) then
                                    corner.CornerRadius = UDim.new(0, 6)
                                end
                            end)
                        end
                    else
                        local c = Instance.new("UICorner")
                        c.CornerRadius = UDim.new(0, 6)
                        c.Parent = btn
                    end

                    for _, c in ipairs(btn:GetDescendants()) do
                        if c:IsA("UICorner") and c.Parent and c.Parent.Name ~= "CyberHudCorners" then
                            c.CornerRadius = UDim.new(0, 6)
                        end
                    end

                    local strk = btn:FindFirstChildOfClass("UIStroke") or Instance.new("UIStroke", btn)
                    strk.Color = strokeColor
                    strk.Thickness = 1
                    strk.ApplyStrokeMode = Enum.ApplyStrokeMode.Border

                    AttachCyberCorners(btn, 4, col or cyanColor, 1)

                    -- Setup interactive hover for standalone buttons
                    local icons = {}
                    for _, img in ipairs(btn:GetDescendants()) do
                        if img:IsA("ImageLabel") or img:IsA("ImageButton") then
                            table.insert(icons, img)
                        end
                    end
                    if btn:IsA("ImageLabel") or btn:IsA("ImageButton") then
                        table.insert(icons, btn)
                    end
                    if #icons > 0 then
                        SetupInteractiveHover(btn, icons, btn, true)
                    end
                end)
            end

            local function ScanActionButtons()
                pcall(function()
                    for _, g in ipairs(guis) do
                        for _, desc in ipairs(g:GetDescendants()) do
                            if desc:IsA("GuiObject") and not IsInsidePill(desc) then
                                local sz = desc.AbsoluteSize
                                local ox = desc.Size.X.Offset
                                local oy = desc.Size.Y.Offset
                                local effW = (sz.X > 0 and sz.X) or ox
                                local effH = (sz.Y > 0 and sz.Y) or oy

                                -- Button size check: 20px to 85px, roughly square
                                if effW >= 20 and effW <= 85 and effH >= 20 and effH <= 85 and math.abs(effW - effH) <= 16 then
                                    local hasCorner = desc:FindFirstChildOfClass("UICorner") or (desc.Parent and desc.Parent:FindFirstChildOfClass("UICorner"))
                                    local img = desc:FindFirstChildOfClass("ImageLabel") or desc:FindFirstChildOfClass("ImageButton") or (desc:IsA("ImageButton") and desc) or (desc:IsA("ImageLabel") and desc)

                                    if hasCorner or img then
                                        local n = (desc.Name .. " " .. (desc.Parent and desc.Parent.Name or "")):lower()
                                        local imgl = img and tostring(img.Image):lower() or ""

                                        local isEmote = n:find("emote") or n:find("dance") or n:find("anim") or imgl:find("emote") or imgl:find("dance") or imgl:find("5107110826") or imgl:find("4988118080") or imgl:find("11504515243") or imgl:find("12405407575")
                                        local isShift = n:find("shift") or n:find("lock") or n:find("mouse") or n:find("crosshair") or n:find("target") or imgl:find("mouse") or imgl:find("shift") or imgl:find("crosshair") or imgl:find("468164082") or imgl:find("6031075938") or imgl:find("6031075929") or imgl:find("6031075917") or imgl:find("10734950309") or imgl:find("13847953315") or imgl:find("5070087515")
                                        local isCam = n:find("cam") or n:find("photo") or n:find("screenshot") or n:find("picture") or imgl:find("cam") or imgl:find("photo") or imgl:find("screenshot") or imgl:find("2592362371") or imgl:find("10734950309")

                                        if isEmote or isShift or isCam then
                                            SkinActionBtn(desc, cyanColor)
                                            if desc.Parent and desc.Parent:IsA("GuiObject") and desc.Parent.AbsoluteSize.X <= 85 and desc.Parent.AbsoluteSize.Y <= 85 then
                                                SkinActionBtn(desc.Parent, cyanColor)
                                            end

                                            -- Sibling action buttons in same row container
                                            local rowParent = desc.Parent
                                            if rowParent and rowParent:IsA("GuiObject") then
                                                for _, sib in ipairs(rowParent:GetChildren()) do
                                                    if sib:IsA("GuiObject") and sib ~= desc and sib.Name ~= "CyberHudCorners" and not IsInsidePill(sib) then
                                                        local ssz = sib.AbsoluteSize
                                                        local sox = sib.Size.X.Offset
                                                        local ew = (ssz.X > 0 and ssz.X) or sox
                                                        if ew >= 20 and ew <= 85 then
                                                            SkinActionBtn(sib, cyanColor)
                                                        end
                                                    end
                                                end
                                                -- In case row container is parent of parent (ButtonWrapper -> Row)
                                                local grandParent = rowParent.Parent
                                                if grandParent and grandParent:IsA("GuiObject") and grandParent.AbsoluteSize.X > 80 then
                                                    for _, wrapper in ipairs(grandParent:GetChildren()) do
                                                        if wrapper:IsA("GuiObject") and wrapper ~= rowParent and wrapper.Name ~= "CyberHudCorners" and not IsInsidePill(wrapper) then
                                                            local wsz = wrapper.AbsoluteSize
                                                            local wox = wrapper.Size.X.Offset
                                                            local wew = (wsz.X > 0 and wsz.X) or wox
                                                            if wew >= 20 and wew <= 85 then
                                                                SkinActionBtn(wrapper, cyanColor)
                                                            end
                                                        end
                                                    end
                                                end
                                            end
                                        end
                                    end
                                end
                            end
                        end
                    end
                end)
            end

            -- 3. THEME TOPBAR UNIBAR: Pill & Roblox Button (Image 1)
            local function SkinUnibarPill(pill)
                if not pill or not pill:IsA("GuiObject") then return end

                pcall(function()
                    pill.ClipsDescendants = false
                    pill.BackgroundColor3 = darkBg
                    pill.BackgroundTransparency = 0.15
                    pill.BorderSizePixel = 0

                    local corner = pill:FindFirstChildOfClass("UICorner")
                    if corner then
                        corner.CornerRadius = UDim.new(0, 6)
                        if not corner:GetAttribute("CyberLocked") then
                            corner:SetAttribute("CyberLocked", true)
                            corner:GetPropertyChangedSignal("CornerRadius"):Connect(function()
                                if corner.CornerRadius ~= UDim.new(0, 6) then
                                    corner.CornerRadius = UDim.new(0, 6)
                                end
                            end)
                        end
                    else
                        local c = Instance.new("UICorner")
                        c.CornerRadius = UDim.new(0, 6)
                        c.Parent = pill
                    end

                    local strk = pill:FindFirstChildOfClass("UIStroke") or Instance.new("UIStroke", pill)
                    strk.Color = strokeColor
                    strk.Thickness = 1
                    strk.ApplyStrokeMode = Enum.ApplyStrokeMode.Border

                    AttachCyberCorners(pill, 4, cyanColor, 1)

                    -- FRAME DROP: Remove any stray CyberHudCorners on children inside the pill!
                    for _, d in ipairs(pill:GetDescendants()) do
                        if d.Name == "CyberHudCorners" and d.Parent ~= pill then
                            d:Destroy()
                        end
                    end

                    -- Any child corners inside pill (except notification badge)
                    for _, c in ipairs(pill:GetDescendants()) do
                        if c:IsA("UICorner") and c.Parent and c.Parent ~= pill and c.Parent.Name ~= "CyberHudCorners" then
                            local isBadge = (c.Parent.Name:lower():find("badge")) or (c.Parent:FindFirstChildOfClass("TextLabel") and c.Parent:FindFirstChildOfClass("TextLabel").Text == "99")
                            if not isBadge then
                                c.CornerRadius = UDim.new(0, 4)
                            end
                        end
                    end

                    -- Setup interactive hover for items inside pill (Frame Drop idle, Bright on Hover)
                    for _, ch in ipairs(pill:GetDescendants()) do
                        if ch:IsA("GuiObject") and ch ~= pill and ch.Parent ~= nil then
                            local isItem = ch:IsA("ImageButton") or ch:IsA("TextButton") or ch:IsA("CanvasGroup")
                                or (ch:IsA("Frame") and (ch:FindFirstChildOfClass("ImageLabel") or ch:FindFirstChildOfClass("ImageButton") or ch.Name:lower():find("item") or ch.Name:lower():find("button") or ch.Name:lower():find("icon")))

                            local isBadge = (ch.Name:lower():find("badge")) or (ch:FindFirstChildOfClass("TextLabel") and ch:FindFirstChildOfClass("TextLabel").Text == "99")

                            if isItem and not isBadge then
                                -- Frame drop: default background transparent
                                ch.BackgroundTransparency = 1
                                ch.BorderSizePixel = 0
                                local s = ch:FindFirstChildOfClass("UIStroke")
                                if s then s.Enabled = false end

                                local icons = {}
                                for _, ic in ipairs(ch:GetDescendants()) do
                                    if ic:IsA("ImageLabel") or ic:IsA("ImageButton") then
                                        table.insert(icons, ic)
                                    elseif ic:IsA("Frame") and (ic.Name:lower():find("bar") or ic.Name:lower():find("line")) then
                                        table.insert(icons, ic)
                                    end
                                end
                                if ch:IsA("ImageLabel") or ch:IsA("ImageButton") then
                                    table.insert(icons, ch)
                                end

                                if #icons > 0 then
                                    SetupInteractiveHover(ch, icons, ch, false)
                                end
                            end
                        end
                    end
                end)
            end

            local function ScanTopbar()
                pcall(function()
                    for _, g in ipairs(guis) do
                        for _, desc in ipairs(g:GetDescendants()) do
                            -- Strategy 1: Badge "99" (or badge label) -> Traverse upward to Unibar Pill & sibling Roblox Button
                            if desc:IsA("TextLabel") and (desc.Text == "99" or desc.Name:lower():find("badge") or (desc.Parent and desc.Parent.Name:lower():find("badge"))) then
                                local badgeHolder = desc.Parent
                                if badgeHolder:IsA("GuiObject") then
                                    local bsz = badgeHolder.AbsoluteSize
                                    local box = badgeHolder.Size.X.Offset
                                    local bw = (bsz.X > 0 and bsz.X) or box
                                    if bw > 0 and bw < 65 then
                                        badgeHolder.BackgroundColor3 = redColor
                                        local stroke = badgeHolder:FindFirstChildOfClass("UIStroke") or Instance.new("UIStroke", badgeHolder)
                                        stroke.Color = Color3.fromRGB(255, 120, 140)
                                        stroke.Thickness = 1
                                        desc.Font = Enum.Font.GothamBold
                                        desc.TextColor3 = Color3.fromRGB(255, 255, 255)

                                        -- Walk up to find the Unibar pill container
                                        local curr = badgeHolder.Parent
                                        for _ = 1, 8 do
                                            if not curr or curr == g then break end
                                            if curr:IsA("GuiObject") then
                                                local sz = curr.AbsoluteSize
                                                local ox = curr.Size.X.Offset
                                                local w = (sz.X > 0 and sz.X) or ox
                                                if w >= 60 and w <= 380 and (curr:FindFirstChildOfClass("UICorner") or curr.BackgroundTransparency < 0.9) then
                                                    SkinUnibarPill(curr)

                                                    -- Check siblings inside curr.Parent for the standalone Roblox button!
                                                    if curr.Parent then
                                                        for _, sib in ipairs(curr.Parent:GetChildren()) do
                                                            if sib:IsA("GuiObject") and sib ~= curr and not IsInsidePill(sib) then
                                                                local ssz = sib.AbsoluteSize
                                                                local sox = sib.Size.X.Offset
                                                                local sw = (ssz.X > 0 and ssz.X) or sox
                                                                if (sw >= 20 and sw <= 65) or sib:FindFirstChildOfClass("UICorner") then
                                                                    SkinActionBtn(sib, cyanColor)
                                                                end
                                                            end
                                                        end
                                                    end
                                                    break
                                                end
                                            end
                                            curr = curr.Parent
                                        end
                                    end
                                end
                            end

                            -- Strategy 2: Direct TopBar containers (Unibar pill & Roblox button by size & position)
                            if desc:IsA("GuiObject") then
                                local pos = desc.AbsolutePosition
                                local sz = desc.AbsoluteSize
                                local ox = desc.Size.X.Offset
                                local oy = desc.Size.Y.Offset
                                local w = (sz.X > 0 and sz.X) or ox
                                local h = (sz.Y > 0 and sz.Y) or oy

                                -- TopBar area is near top of screen (Y < 70)
                                if pos.Y <= 70 and pos.X <= 450 then
                                    local hasCorner = desc:FindFirstChildOfClass("UICorner")
                                    if hasCorner then
                                        -- Unibar capsule pill
                                        if w >= 65 and w <= 350 and h >= 24 and h <= 55 then
                                            SkinUnibarPill(desc)
                                        -- Circular Roblox menu button or topbar icon (NOT inside a pill!)
                                        elseif w >= 24 and w <= 65 and h >= 24 and h <= 65 and math.abs(w - h) <= 10 then
                                            if not IsInsidePill(desc) then
                                                SkinActionBtn(desc, cyanColor)
                                            end
                                        end
                                    end
                                end

                                -- Strategy 3: Search by Roblox logo icon asset
                                local n = desc.Name:lower()
                                if desc:IsA("ImageLabel") or desc:IsA("ImageButton") then
                                    local imgl = tostring(desc.Image):lower()
                                    if imgl:find("roblox") or imgl:find("coloredlogo") or imgl:find("roblox_tile") or n:find("roblox") then
                                        local btn = desc:FindFirstAncestorOfClass("ImageButton") or desc:FindFirstAncestorOfClass("TextButton") or desc.Parent
                                        if btn and btn:IsA("GuiObject") and btn.AbsoluteSize.X <= 65 and not IsInsidePill(btn) then
                                            SkinActionBtn(btn, cyanColor)
                                        end
                                    end
                                end
                            end
                        end
                    end
                end)
            end

            -- Initial pass
            ScanHotbar()
            ScanActionButtons()
            ScanTopbar()

            -- Responsive on player respawn
            Player.CharacterAdded:Connect(function()
                task.wait(1)
                ScanHotbar()
                ScanActionButtons()
                ScanTopbar()
            end)

            -- Fast observer passes for late loading assets
            for _ = 1, 6 do
                task.wait(1)
                ScanHotbar()
                ScanActionButtons()
                ScanTopbar()
            end

            -- Continuous heartbeat (every 3 seconds) for dynamic UI updates & respawns
            task.spawn(function()
                while task.wait(3) do
                    pcall(function()
                        ScanHotbar()
                        ScanActionButtons()
                        ScanTopbar()
                    end)
                end
            end)
        end)
    end
end

-- Helper: High-Performance Draggable Frame (Smooth 60/144+ FPS, zero stutter)
local function MakeDraggable(dragHandle, targetFrame)
    local dragging = false
    local dragStart = nil
    local startPos = nil
    local targetPos = nil

    local moveConn = nil
    local renderConn = nil
    local endConn = nil
    local focusConn = nil

    local function stopDragging()
        dragging = false
        if moveConn then moveConn:Disconnect() moveConn = nil end
        if renderConn then renderConn:Disconnect() renderConn = nil end
        if endConn then endConn:Disconnect() endConn = nil end
        if focusConn then focusConn:Disconnect() focusConn = nil end
        if targetPos and targetFrame and targetFrame.Parent then
            targetFrame.Position = targetPos
        end
        dragStart = nil
        startPos = nil
        targetPos = nil
    end

    dragHandle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            stopDragging()
            dragging = true
            dragStart = input.Position
            startPos = targetFrame.Position
            targetPos = startPos

            moveConn = UserInputService.InputChanged:Connect(function(moveInput)
                if dragging and (moveInput.UserInputType == Enum.UserInputType.MouseMovement or moveInput.UserInputType == Enum.UserInputType.Touch) then
                    local delta = moveInput.Position - dragStart
                    targetPos = UDim2.new(
                        startPos.X.Scale, startPos.X.Offset + delta.X,
                        startPos.Y.Scale, startPos.Y.Offset + delta.Y
                    )
                end
            end)

            renderConn = RunService.RenderStepped:Connect(function()
                if dragging and targetPos and targetFrame and targetFrame.Parent then
                    if targetFrame.Position ~= targetPos then
                        targetFrame.Position = targetPos
                    end
                else
                    stopDragging()
                end
            end)

            endConn = UserInputService.InputEnded:Connect(function(endInput)
                if endInput.UserInputType == Enum.UserInputType.MouseButton1 or endInput.UserInputType == Enum.UserInputType.Touch then
                    stopDragging()
                end
            end)

            focusConn = UserInputService.WindowFocusReleased:Connect(stopDragging)
        end
    end)
end

-- Helper: Circle click / Ripple feedback (Supports Chamfer Cards & Standard Frames)
local function RippleClick(Button, cardData)
    if cardData and cardData.SetFillColor then
        cardData.SetFillColor(Color3.fromRGB(36, 46, 68), 0.08)
        task.delay(0.12, function()
            pcall(function()
                cardData.SetFillColor(Custom.CardDark, 0.15)
            end)
        end)
    else
        local orig = Button.BackgroundColor3
        TweenService:Create(Button, TweenInfo.new(0.08, Enum.EasingStyle.Quad), {BackgroundColor3 = Color3.fromRGB(50, 40, 70)}):Play()
        task.delay(0.12, function()
            pcall(function()
                if Button and Button.Parent then
                    TweenService:Create(Button, TweenInfo.new(0.15, Enum.EasingStyle.Quad), {BackgroundColor3 = orig}):Play()
                end
            end)
        end)
    end
end

-- ===============================================================
-- ASUS ROG CYBERPUNK 3-BUTTON FLOATING DOCK (Anti-Bulat / Chamfered)
-- Exactly matching the ASUS ROG Armoury Crate Bottom Deck:
-- 1. Left: Discord ("Official site" • Chamfered Hexagon, Cyan slit)
-- 2. Center: Buka Dashboard ("Scenario Profiles" • Chamfered Hexagon, Glowing Red slit)
-- 3. Right: Online ("My record" • Chamfered Hexagon, Cyan slit)
-- ===============================================================
local function CreateOpenCloseButton()
    local ScreenGui = Custom:Create("ScreenGui", {
        Name = "Shield_Toggle",
        ZIndexBehavior = Enum.ZIndexBehavior.Sibling,
        ResetOnSpawn = false
    }, RunService:IsStudio() and Player.PlayerGui or (gethui and gethui() or game:GetService("CoreGui")))
    ProtectGui(ScreenGui)

    -- Dock Container Frame (Positioned at TOP as requested: "taro di atas jangan di bawah")
    local DockHolder = Custom:Create("Frame", {
        BackgroundTransparency = 1,
        AnchorPoint = Vector2.new(0.5, 0),
        Position = UDim2.new(0.5, 0, 0, 12),
        Size = UDim2.fromOffset(430, 48),
        ZIndex = 50,
        Name = "Shield_DockHolder"
    }, ScreenGui)

    -- Bottom Cyberpunk Grip Handle (for dragging smoothly anywhere on screen)
    local GripBar = Custom:Create("Frame", {
        BackgroundColor3 = Color3.fromRGB(18, 24, 36),
        BackgroundTransparency = 0.2,
        BorderSizePixel = 0,
        AnchorPoint = Vector2.new(0.5, 0),
        Position = UDim2.new(0.5, 0, 1, 4),
        Size = UDim2.fromOffset(80, 5),
        ZIndex = 51,
        Name = "GripBar"
    }, DockHolder)
    Custom:Create("UICorner", { CornerRadius = UDim.new(1, 0) }, GripBar)
    local GripLine = Custom:Create("Frame", {
        BackgroundColor3 = Color3.fromRGB(255, 45, 75),
        BorderSizePixel = 0,
        AnchorPoint = Vector2.new(0.5, 0.5),
        Position = UDim2.new(0.5, 0, 0.5, 0),
        Size = UDim2.fromOffset(28, 2),
        ZIndex = 52
    }, GripBar)
    Custom:Create("UICorner", { CornerRadius = UDim.new(1, 0) }, GripLine)

    -- Precision ASUS ROG Chamfered Box Builder (45-degree Beveled Cut Corners)
    local function CreateChamferBox(parent, size, pos, anchor, chamfer, strokeColor, fillColor, slitColor, zIndex, topColor, bottomColor)
        local root, cardData = Custom:CreateChamferCard({
            Parent = parent,
            Size = size,
            Position = pos,
            AnchorPoint = anchor or Vector2.new(0, 0),
            Chamfer = chamfer or 6,
            StrokeColor = strokeColor,
            FillColor = fillColor,
            StrokeThickness = 1.2,
            ZIndex = zIndex or 2,
            Name = "ChamferBox"
        })

        if topColor and bottomColor and cardData.TL_Fill then
            cardData.TL_Fill.BackgroundColor3 = topColor
            cardData.TR_Fill.BackgroundColor3 = topColor
            cardData.BL_Fill.BackgroundColor3 = bottomColor
            cardData.BR_Fill.BackgroundColor3 = bottomColor
        end

        -- Bottom Glowing Neon Slit (Signature ROG Slit)
        local slit = Custom:Create("Frame", {
            BackgroundColor3 = slitColor,
            BorderSizePixel = 0,
            AnchorPoint = Vector2.new(0.5, 1),
            Position = UDim2.new(0.5, 0, 1, -1),
            Size = UDim2.new(0.6, 0, 0, 2.5),
            ZIndex = (zIndex or 2) + 3,
            Name = "Slit"
        }, root)
        Custom:Create("UICorner", { CornerRadius = UDim.new(1, 0) }, slit)

        return root, slit, cardData.CenterH, cardData.CenterV
    end

    -- Universal Draggable & Click Handler (Works smoothly for PC & Mobile)
    local function SetupButtonInteraction(btn, rootBox, slit, baseSize, hoverSize, strokeColor, slitColor, onClick)
        local dragStart = nil
        local startPos = nil
        local targetPos = nil
        local isDragging = false
        local moveConn = nil
        local renderConn = nil
        local endConn = nil
        local focusConn = nil

        local function cleanupDrag()
            if moveConn then moveConn:Disconnect() moveConn = nil end
            if renderConn then renderConn:Disconnect() renderConn = nil end
            if endConn then endConn:Disconnect() endConn = nil end
            if focusConn then focusConn:Disconnect() focusConn = nil end
            if targetPos and DockHolder and DockHolder.Parent then
                DockHolder.Position = targetPos
            end
            dragStart = nil
            startPos = nil
            targetPos = nil
        end

        btn.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                cleanupDrag()
                dragStart = input.Position
                startPos = DockHolder.Position
                targetPos = startPos
                isDragging = false

                moveConn = UserInputService.InputChanged:Connect(function(moveInput)
                    if (moveInput.UserInputType == Enum.UserInputType.MouseMovement or moveInput.UserInputType == Enum.UserInputType.Touch) and dragStart then
                        local delta = moveInput.Position - dragStart
                        if delta.Magnitude > 6 then
                            isDragging = true
                        end
                        if isDragging then
                            targetPos = UDim2.new(
                                startPos.X.Scale, startPos.X.Offset + delta.X,
                                startPos.Y.Scale, startPos.Y.Offset + delta.Y
                            )
                        end
                    end
                end)

                renderConn = RunService.RenderStepped:Connect(function()
                    if isDragging and targetPos and DockHolder and DockHolder.Parent then
                        if DockHolder.Position ~= targetPos then
                            DockHolder.Position = targetPos
                        end
                    end
                end)

                endConn = UserInputService.InputEnded:Connect(function(endInput)
                    if endInput.UserInputType == Enum.UserInputType.MouseButton1 or endInput.UserInputType == Enum.UserInputType.Touch then
                        local wasDrag = isDragging
                        cleanupDrag()
                        if not wasDrag and onClick then
                            onClick()
                        end
                        isDragging = false
                    end
                end)

                focusConn = UserInputService.WindowFocusReleased:Connect(function()
                    cleanupDrag()
                    isDragging = false
                end)
            end
        end)

        btn.MouseEnter:Connect(function()
            TweenService:Create(rootBox, TweenInfo.new(0.15), {Size = hoverSize}):Play()
            TweenService:Create(slit, TweenInfo.new(0.15), {Size = UDim2.new(0.85, 0, 0, 3.5), BackgroundColor3 = Color3.fromRGB(255, 255, 255)}):Play()
        end)
        btn.MouseLeave:Connect(function()
            TweenService:Create(rootBox, TweenInfo.new(0.15), {Size = baseSize}):Play()
            TweenService:Create(slit, TweenInfo.new(0.15), {Size = UDim2.new(0.6, 0, 0, 2.5), BackgroundColor3 = slitColor}):Play()
        end)
    end

    -- ==========================================
    -- BUTTON 1: DISCORD ("Official site") - LEFT
    -- ==========================================
    local leftBaseSize = UDim2.fromOffset(125, 40)
    local leftHoverSize = UDim2.fromOffset(128, 42)
    local LeftBox, LeftSlit = CreateChamferBox(
        DockHolder,
        leftBaseSize,
        UDim2.new(0.5, -90, 0.5, 0),
        Vector2.new(1, 0.5),
        7,
        Color3.fromRGB(0, 195, 255),
        Color3.fromRGB(12, 16, 26),
        Color3.fromRGB(0, 195, 255),
        10
    )

    -- Discord Icon
    Custom:Create("ImageLabel", {
        Image = "rbxassetid://10734950309",
        ImageColor3 = Color3.fromRGB(0, 210, 255),
        BackgroundTransparency = 1,
        AnchorPoint = Vector2.new(0, 0.5),
        Position = UDim2.new(0, 10, 0.5, 0),
        Size = UDim2.fromOffset(18, 18),
        ZIndex = 14
    }, LeftBox)

    Custom:Create("TextLabel", {
        Font = Enum.Font.GothamBold,
        Text = "Discord",
        TextColor3 = Color3.fromRGB(240, 245, 255),
        TextSize = 11,
        TextXAlignment = Enum.TextXAlignment.Left,
        BackgroundTransparency = 1,
        Position = UDim2.new(0, 34, 0, 6),
        Size = UDim2.new(1, -36, 0, 13),
        ZIndex = 14
    }, LeftBox)

    Custom:Create("TextLabel", {
        Font = Enum.Font.Gotham,
        Text = "Official site",
        TextColor3 = Color3.fromRGB(0, 195, 255),
        TextSize = 8.5,
        TextXAlignment = Enum.TextXAlignment.Left,
        BackgroundTransparency = 1,
        Position = UDim2.new(0, 34, 0, 20),
        Size = UDim2.new(1, -36, 0, 11),
        ZIndex = 14
    }, LeftBox)

    local LeftClickBtn = Custom:Create("TextButton", {
        AutoButtonColor = false,
        BackgroundTransparency = 1,
        Text = "",
        Size = UDim2.new(1, 0, 1, 0),
        ZIndex = 20,
        Name = "LeftDiscordBtn"
    }, LeftBox)

    SetupButtonInteraction(
        LeftClickBtn, LeftBox, LeftSlit, leftBaseSize, leftHoverSize,
        Color3.fromRGB(0, 195, 255), Color3.fromRGB(0, 195, 255),
        function()
            pcall(function()
                if setclipboard then setclipboard("https://discord.gg/shieldteam") end
            end)
            if Speed_Library and Speed_Library.SetNotification then
                Speed_Library:SetNotification({
                    Title = "Official Site",
                    Content = "Discord link copied to clipboard!\n(discord.gg/shieldteam)",
                    Delay = 3
                })
            end
        end
    )

    -- ==================================================
    -- BUTTON 2: BUKA DASHBOARD ("Scenario Profiles") - CENTER
    -- ==================================================
    local centerBaseSize = UDim2.fromOffset(160, 46)
    local centerHoverSize = UDim2.fromOffset(166, 49)
    local CenterBox, CenterSlit, CH, CV = CreateChamferBox(
        DockHolder,
        centerBaseSize,
        UDim2.new(0.5, 0, 0.5, 0),
        Vector2.new(0.5, 0.5),
        8,
        Color3.fromRGB(255, 45, 75),
        Color3.fromRGB(140, 14, 32),
        Color3.fromRGB(255, 45, 75),
        15,
        Color3.fromRGB(165, 18, 38), -- Top Ruby color matching gradient
        Color3.fromRGB(85, 10, 22)    -- Bottom Ruby color matching gradient
    )

    -- Ruby Red Gradient for Center Button
    local Grad1 = Custom:Create("UIGradient", {
        Color = ColorSequence.new({
            ColorSequenceKeypoint.new(0, Color3.fromRGB(165, 18, 38)),
            ColorSequenceKeypoint.new(1, Color3.fromRGB(85, 10, 22))
        }),
        Rotation = 90
    }, CH)
    Grad1:Clone().Parent = CV

    -- Center ROG Icon
    Custom:Create("ImageLabel", {
        Image = "rbxassetid://10709768641",
        ImageColor3 = Color3.fromRGB(255, 255, 255),
        BackgroundTransparency = 1,
        AnchorPoint = Vector2.new(0, 0.5),
        Position = UDim2.new(0, 12, 0.5, 0),
        Size = UDim2.fromOffset(20, 20),
        ZIndex = 18
    }, CenterBox)

    Custom:Create("TextLabel", {
        Font = Enum.Font.GothamBold,
        Text = "Buka Dashboard",
        TextColor3 = Color3.fromRGB(255, 255, 255),
        TextSize = 12,
        TextXAlignment = Enum.TextXAlignment.Left,
        BackgroundTransparency = 1,
        Position = UDim2.new(0, 38, 0, 7),
        Size = UDim2.new(1, -40, 0, 15),
        ZIndex = 18
    }, CenterBox)

    Custom:Create("TextLabel", {
        Font = Enum.Font.Gotham,
        Text = "Scenario Profiles",
        TextColor3 = Color3.fromRGB(255, 175, 190),
        TextSize = 8.5,
        TextXAlignment = Enum.TextXAlignment.Left,
        BackgroundTransparency = 1,
        Position = UDim2.new(0, 38, 0, 23),
        Size = UDim2.new(1, -40, 0, 11),
        ZIndex = 18
    }, CenterBox)

    local CenterClickBtn = Custom:Create("TextButton", {
        AutoButtonColor = false,
        BackgroundTransparency = 1,
        Text = "",
        Size = UDim2.new(1, 0, 1, 0),
        ZIndex = 25,
        Name = "RestoreButton"
    }, CenterBox)

    local lastToggle = 0
    SetupButtonInteraction(
        CenterClickBtn, CenterBox, CenterSlit, centerBaseSize, centerHoverSize,
        Color3.fromRGB(255, 45, 75), Color3.fromRGB(255, 45, 75),
        function()
            local now = tick()
            if now - lastToggle > 0.25 then
                lastToggle = now
                if Speed_Library and Speed_Library.ActiveWindow and Speed_Library.ActiveWindow.Toggle then
                    Speed_Library.ActiveWindow:Toggle()
                end
            end
        end
    )

    -- Auto destroy ScreenGui when CenterClickBtn is destroyed
    CenterClickBtn.AncestryChanged:Connect(function(_, parent)
        if not parent and ScreenGui and ScreenGui.Parent then
            ScreenGui:Destroy()
        end
    end)

    -- ==========================================
    -- BUTTON 3: ONLINE ("My record") - RIGHT
    -- ==========================================
    local rightBaseSize = UDim2.fromOffset(125, 40)
    local rightHoverSize = UDim2.fromOffset(128, 42)
    local RightBox, RightSlit = CreateChamferBox(
        DockHolder,
        rightBaseSize,
        UDim2.new(0.5, 90, 0.5, 0),
        Vector2.new(0, 0.5),
        7,
        Color3.fromRGB(0, 195, 255),
        Color3.fromRGB(12, 16, 26),
        Color3.fromRGB(0, 195, 255),
        10
    )

    -- Online Icon (Signal / Radar)
    Custom:Create("ImageLabel", {
        Image = "rbxassetid://10709768641",
        ImageColor3 = Color3.fromRGB(0, 210, 255),
        BackgroundTransparency = 1,
        AnchorPoint = Vector2.new(0, 0.5),
        Position = UDim2.new(0, 10, 0.5, 0),
        Size = UDim2.fromOffset(18, 18),
        ZIndex = 14
    }, RightBox)

    local OnlineTitle = Custom:Create("TextLabel", {
        Font = Enum.Font.GothamBold,
        Text = "Online",
        TextColor3 = Color3.fromRGB(240, 245, 255),
        TextSize = 11,
        TextXAlignment = Enum.TextXAlignment.Left,
        BackgroundTransparency = 1,
        Position = UDim2.new(0, 34, 0, 6),
        Size = UDim2.new(1, -36, 0, 13),
        ZIndex = 14
    }, RightBox)

    Custom:Create("TextLabel", {
        Font = Enum.Font.Gotham,
        Text = "My record",
        TextColor3 = Color3.fromRGB(0, 195, 255),
        TextSize = 8.5,
        TextXAlignment = Enum.TextXAlignment.Left,
        BackgroundTransparency = 1,
        Position = UDim2.new(0, 34, 0, 20),
        Size = UDim2.new(1, -36, 0, 11),
        ZIndex = 14
    }, RightBox)

    local function UpdateOnlineCount()
        pcall(function()
            local count = #Players:GetPlayers()
            OnlineTitle.Text = "Online (" .. tostring(count) .. ")"
        end)
    end
    UpdateOnlineCount()
    Players.PlayerAdded:Connect(UpdateOnlineCount)
    Players.PlayerRemoving:Connect(UpdateOnlineCount)

    local RightClickBtn = Custom:Create("TextButton", {
        AutoButtonColor = false,
        BackgroundTransparency = 1,
        Text = "",
        Size = UDim2.new(1, 0, 1, 0),
        ZIndex = 20,
        Name = "RightRecordBtn"
    }, RightBox)

    SetupButtonInteraction(
        RightClickBtn, RightBox, RightSlit, rightBaseSize, rightHoverSize,
        Color3.fromRGB(0, 195, 255), Color3.fromRGB(0, 195, 255),
        function()
            if Speed_Library and Speed_Library.SetNotification then
                local pingVal = 45
                pcall(function()
                    pingVal = math.floor(game:GetService("Stats").Network.ServerStatsItem["Data Ping"]:GetValue())
                end)
                Speed_Library:SetNotification({
                    Title = "My Record & Status",
                    Content = "Account: " .. (Player and Player.Name or "shieldaateam") .. "\nOnline: " .. #Players:GetPlayers() .. " Players • Ping: " .. pingVal .. " ms\nProfile: ROG Performance Active",
                    Delay = 3.5
                })
            end
        end
    )

    MakeDraggable(GripBar, DockHolder)

    return CenterClickBtn
end

local GlobalOpenClose = CreateOpenCloseButton()

-- Speed_Library Definition
local Speed_Library = { Unloaded = false }
local GlobalNotificationGui = nil
local GlobalNotificationLayout = nil

function Speed_Library:SetNotification(Config)
    local Title = Config[1] or Config.Title or "Notification"
    local Description = Config[2] or Config.Description or ""
    local Content = Config[3] or Config.Content or ""
    local Delay = Config[6] or Config.Delay or 4

    if not GlobalNotificationGui or not GlobalNotificationGui.Parent then
        GlobalNotificationGui = Custom:Create("ScreenGui", {
            ZIndexBehavior = Enum.ZIndexBehavior.Sibling,
            Name = "ShieldNotificationGui"
        }, RunService:IsStudio() and Player.PlayerGui or (gethui and gethui() or game:GetService("CoreGui")))
        ProtectGui(GlobalNotificationGui)

        GlobalNotificationLayout = Custom:Create("Frame", {
            AnchorPoint = Vector2.new(1, 1),
            BackgroundTransparency = 1,
            Position = UDim2.new(1, -20, 1, -20),
            Size = UDim2.new(0, 300, 1, 0),
            Name = "NotificationLayout"
        }, GlobalNotificationGui)

        local Count = 0
        GlobalNotificationLayout.ChildRemoved:Connect(function()
            Count = 0
            for _, v in ipairs(GlobalNotificationLayout:GetChildren()) do
                local newPos = UDim2.new(0, 0, 1, -((v.Size.Y.Offset + 8) * Count))
                TweenService:Create(v, TweenInfo.new(0.25, Enum.EasingStyle.Quad), {Position = newPos}):Play()
                Count = Count + 1
            end
        end)
    end

    local countOffset = 0
    for _, v in ipairs(GlobalNotificationLayout:GetChildren()) do
        countOffset = -(v.Position.Y.Offset) + v.Size.Y.Offset + 8
    end

    local NotifFrame = Custom:Create("Frame", {
        BackgroundColor3 = Color3.fromRGB(15, 14, 20),
        BorderSizePixel = 0,
        Size = UDim2.new(1, 0, 0, 65),
        AnchorPoint = Vector2.new(0, 1),
        Position = UDim2.new(0, 350, 1, -countOffset),
        Name = "NotifFrame"
    }, GlobalNotificationLayout)

    Custom:Create("UICorner", { CornerRadius = UDim.new(0, 8) }, NotifFrame)
    Custom:Create("UIStroke", { Color = Color3.fromRGB(255, 45, 75), Thickness = 1, Transparency = 0.4 }, NotifFrame)

    Custom:Create("TextLabel", {
        Font = Enum.Font.GothamBold,
        Text = Title,
        TextColor3 = Color3.fromRGB(245, 245, 250),
        TextSize = 13,
        TextXAlignment = Enum.TextXAlignment.Left,
        BackgroundTransparency = 1,
        Position = UDim2.new(0, 12, 0, 8),
        Size = UDim2.new(1, -24, 0, 16),
        Parent = NotifFrame
    })

    Custom:Create("TextLabel", {
        Font = Enum.Font.Gotham,
        Text = Content ~= "" and Content or Description,
        TextColor3 = Color3.fromRGB(160, 170, 190),
        TextSize = 11,
        TextXAlignment = Enum.TextXAlignment.Left,
        TextYAlignment = Enum.TextYAlignment.Top,
        TextWrapped = true,
        BackgroundTransparency = 1,
        Position = UDim2.new(0, 12, 0, 28),
        Size = UDim2.new(1, -24, 0, 30),
        Parent = NotifFrame
    })

    TweenService:Create(NotifFrame, TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
        Position = UDim2.new(0, 0, 1, -countOffset)
    }):Play()

    task.delay(Delay, function()
        pcall(function()
            if NotifFrame and NotifFrame.Parent then
                local hide = TweenService:Create(NotifFrame, TweenInfo.new(0.25, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {
                    Position = UDim2.new(0, 350, 1, NotifFrame.Position.Y.Offset)
                })
                hide:Play()
                hide.Completed:Wait()
                NotifFrame:Destroy()
            end
        end)
    end)
end

function Speed_Library:CreateWindow(Config)
    Config = Config or {}
    local Title = Config[1] or Config.Title or "Shield Onyx Project"
    local detectedGame = "Fisch"
    pcall(function()
        local placeId = game.PlaceId
        if placeId == 16732694052 or placeId == 18565514941 or (game.Name and string.find(string.lower(game.Name), "fisch")) then
            detectedGame = "Fisch"
        elseif placeId == 2753915549 or placeId == 4442272183 or placeId == 7449423635 or (game.Name and string.find(string.lower(game.Name), "blox")) then
            detectedGame = "Blox Fruit"
        elseif game.Name and game.Name ~= "" then
            detectedGame = game.Name
        end
    end)
    local Description = Config[2] or Config.Description or Config.SubTitle or (detectedGame .. " • Shield Edition • " .. os.date("%A"))
    local SizeUi = Config[4] or Config.SizeUi or UDim2.fromOffset(660, 420)
    local LoadMode = Config[5] or Config.LoadMode or "fast"

    local BaseWidth = SizeUi.X.Offset
    local BaseHeight = SizeUi.Y.Offset

    local GuiParent = RunService:IsStudio() and Player.PlayerGui or (gethui and gethui() or game:GetService("CoreGui"))
    local MainGui = Custom:Create("ScreenGui", {
        Name = "Shield_UI",
        ZIndexBehavior = Enum.ZIndexBehavior.Sibling,
        ResetOnSpawn = false
    }, GuiParent)
    ProtectGui(MainGui)
    pcall(function() Custom:ApplyGameHudTheme() end)

    -- Drop Shadow Container
    local InitialVisible = (Config.Visible ~= false)
    local DropShadowHolder = Custom:Create("Frame", {
        BackgroundColor3 = Custom.BackgroundDark,
        BackgroundTransparency = 1,
        BorderSizePixel = 0,
        Size = SizeUi,
        AnchorPoint = Vector2.new(0.5, 0.5),
        Position = UDim2.new(0.5, 0, 0.5, 0),
        Visible = InitialVisible,
        Name = "DropShadowHolder"
    }, MainGui)

    local GlobalUIScale = Custom:Create("UIScale", {
        Scale = 1,
        Name = "GlobalUIScale"
    }, DropShadowHolder)

    local DropShadow = Custom:Create("ImageLabel", {
        Image = "rbxassetid://6015897843",
        ImageColor3 = Color3.fromRGB(0, 0, 0),
        ImageTransparency = 0.35,
        ScaleType = Enum.ScaleType.Slice,
        SliceCenter = Rect.new(49, 49, 450, 450),
        AnchorPoint = Vector2.new(0.5, 0.5),
        BackgroundTransparency = 1,
        BorderSizePixel = 0,
        Position = UDim2.new(0.5, 0, 0.5, 0),
        Size = UDim2.new(1, 40, 1, 40),
        ZIndex = 1,
        Name = "DropShadow"
    }, DropShadowHolder)

    -- Main Window Frame (Precision Chamfered ROG Cyberpunk Chassis)
    local Main, MainCardData = Custom:CreateChamferCard({
        Parent = DropShadowHolder,
        Size = UDim2.new(1, 0, 1, 0),
        Position = UDim2.new(0.5, 0, 0.5, 0),
        AnchorPoint = Vector2.new(0.5, 0.5),
        Chamfer = 10,
        FillColor = Custom.BackgroundDark,
        StrokeColor = Color3.fromRGB(45, 56, 80),
        CornerColor = Color3.fromRGB(255, 45, 75),
        CornerThickness = 1,
        CornerTransparency = 0.25,
        StrokeThickness = 1,
        ZIndex = 2,
        Name = "MainFrame"
    })

    -- ─────────────────────────────────────────────────────────────
    -- TOP HEADER (Title, Subtitle, Settings, Credits, Resize, Close)
    -- ─────────────────────────────────────────────────────────────
    local Topbar = Custom:Create("Frame", {
        BackgroundColor3 = Color3.fromRGB(15, 15, 20),
        BackgroundTransparency = 1,
        BorderSizePixel = 0,
        Position = UDim2.new(0, 0, 0, 0),
        Size = UDim2.new(1, 0, 0, 42),
        ZIndex = 3,
        Name = "Topbar"
    }, Main)

    MakeDraggable(Topbar, DropShadowHolder)

    -- Shield Logo Left
    local ROGHeaderLogo = Custom:Create("ImageLabel", {
        Image = "rbxassetid://10709768641",
        ImageColor3 = Color3.fromRGB(255, 45, 75),
        BackgroundTransparency = 1,
        AnchorPoint = Vector2.new(0.5, 0.5),
        Position = UDim2.new(0, 14, 0.5, 0),
        Size = UDim2.fromOffset(20, 20),
        ZIndex = 5,
        Name = "ShieldHeaderLogo"
    }, Topbar)

    -- Title Left
    local TitleLabel = Custom:Create("TextLabel", {
        Font = Enum.Font.GothamBold,
        Text = Title,
        TextColor3 = Color3.fromRGB(255, 255, 255),
        TextSize = 14,
        TextXAlignment = Enum.TextXAlignment.Left,
        BackgroundTransparency = 1,
        Position = UDim2.new(0, 40, 0, 6),
        Size = UDim2.new(0, 210, 0, 16),
        Name = "TitleLabel",
        ZIndex = 4
    }, Topbar)

    -- Subtitle Left (ROG Cyber Red)
    local SubtitleLabel = Custom:Create("TextLabel", {
        Font = Enum.Font.GothamBold,
        Text = Description,
        TextColor3 = Color3.fromRGB(255, 65, 95),
        TextSize = 11,
        TextXAlignment = Enum.TextXAlignment.Left,
        BackgroundTransparency = 1,
        Position = UDim2.new(0, 40, 0, 24),
        Size = UDim2.new(0, 210, 0, 14),
        Name = "SubtitleLabel",
        ZIndex = 4
    }, Topbar)

    -- Signature ROG Red Glow Divider
    local TopRedGlow = Custom:Create("Frame", {
        BackgroundColor3 = Color3.fromRGB(255, 45, 75),
        BorderSizePixel = 0,
        Position = UDim2.new(0, 0, 1, -1),
        Size = UDim2.new(1, 0, 0, 1.5),
        ZIndex = 5,
        Name = "TopRedGlow"
    }, Topbar)

    -- Top Right Action Container
    local TopRightActions = Custom:Create("Frame", {
        AnchorPoint = Vector2.new(1, 0.5),
        BackgroundTransparency = 1,
        Position = UDim2.new(1, -12, 0.5, 0),
        Size = UDim2.new(0, 380, 0, 28),
        ZIndex = 4,
        Name = "TopRightActions"
    }, Topbar)

    local ActionsLayout = Custom:Create("UIListLayout", {
        FillDirection = Enum.FillDirection.Horizontal,
        HorizontalAlignment = Enum.HorizontalAlignment.Right,
        VerticalAlignment = Enum.VerticalAlignment.Center,
        Padding = UDim.new(0, 6),
        SortOrder = Enum.SortOrder.LayoutOrder
    }, TopRightActions)

    -- Helper: Create Pill Header Button (Games / Settings / Credits in ROG Cyber Chamfered Style)
    local function CreatePillButton(text, iconAsset, order, isRed)
        local strokeCol = isRed and Color3.fromRGB(255, 45, 75) or Color3.fromRGB(0, 195, 255)
        local iconCol = isRed and Color3.fromRGB(255, 55, 85) or Color3.fromRGB(0, 210, 255)

        local btnFrame, btnCardData = Custom:CreateChamferCard({
            Parent = TopRightActions,
            Size = UDim2.new(0, 82, 0, 26),
            LayoutOrder = order,
            Chamfer = 5,
            FillColor = Color3.fromRGB(14, 18, 28),
            StrokeColor = strokeCol,
            StrokeThickness = 1,
            ZIndex = 4,
            Name = text .. "BtnHolder"
        })

        local btn = Custom:Create("TextButton", {
            Text = "",
            BackgroundTransparency = 1,
            Size = UDim2.new(1, 0, 1, 0),
            ZIndex = 6,
            Name = "ClickBtn"
        }, btnFrame)

        local icon = Custom:Create("ImageLabel", {
            Image = iconAsset,
            ImageColor3 = iconCol,
            BackgroundTransparency = 1,
            Position = UDim2.new(0, 7, 0.5, -7),
            Size = UDim2.new(0, 14, 0, 14),
            ZIndex = 5
        }, btnFrame)

        local lbl = Custom:Create("TextLabel", {
            Font = Enum.Font.GothamBold,
            Text = text,
            TextColor3 = Color3.fromRGB(235, 240, 255),
            TextSize = 11,
            TextXAlignment = Enum.TextXAlignment.Left,
            BackgroundTransparency = 1,
            Position = UDim2.new(0, 25, 0.5, -6),
            Size = UDim2.new(1, -28, 0, 13),
            ZIndex = 5
        }, btnFrame)

        local slit = Custom:Create("Frame", {
            BackgroundColor3 = strokeCol,
            BorderSizePixel = 0,
            AnchorPoint = Vector2.new(0.5, 1),
            Position = UDim2.new(0.5, 0, 1, 0),
            Size = UDim2.new(0.5, 0, 0, 2),
            ZIndex = 6
        }, btnFrame)
        Custom:Create("UICorner", { CornerRadius = UDim.new(1, 0) }, slit)

        btn.MouseEnter:Connect(function()
            btnCardData.SetFillColor(Color3.fromRGB(24, 30, 46), 0.15)
            TweenService:Create(slit, TweenInfo.new(0.15), {Size = UDim2.new(0.85, 0, 0, 2.5), BackgroundColor3 = Color3.fromRGB(255, 255, 255)}):Play()
        end)
        btn.MouseLeave:Connect(function()
            btnCardData.SetFillColor(Color3.fromRGB(14, 18, 28), 0.15)
            TweenService:Create(slit, TweenInfo.new(0.15), {Size = UDim2.new(0.5, 0, 0, 2), BackgroundColor3 = strokeCol}):Play()
        end)

        return btn, btnFrame
    end

    local SettingsBtn, SettingsFrame = CreatePillButton("Settings", "rbxassetid://7072721867", 1, false)
    local CreditsBtn, CreditsFrame = CreatePillButton("Credits", "rbxassetid://10747373176", 2, false)

    -- ─────────────────────────────────────────────────────────────
    -- ROG WINDOW CONTROL BUTTONS (Minimize [—], Resize [↗], Close [X])
    -- ─────────────────────────────────────────────────────────────
    local function CreateWindowControlBtn(order, name)
        local btn = Custom:Create("TextButton", {
            Text = "",
            BackgroundColor3 = Color3.fromRGB(18, 22, 32),
            BorderSizePixel = 0,
            Size = UDim2.new(0, 26, 0, 24),
            LayoutOrder = order,
            ZIndex = 6,
            Name = name
        }, TopRightActions)

        Custom:Create("UICorner", {
            CornerRadius = UDim.new(0, 4),
            Parent = btn
        })

        local stroke = Custom:Create("UIStroke", {
            Color = Color3.fromRGB(36, 46, 66),
            Thickness = 1,
            ApplyStrokeMode = Enum.ApplyStrokeMode.Border,
            Parent = btn
        })

        return btn, stroke
    end

    -- 1. Minimize Button (—)
    local MinimizeBtn, MinStroke = CreateWindowControlBtn(3, "MinimizeBtn")
    local MinBar = Custom:Create("Frame", {
        BackgroundColor3 = Color3.fromRGB(205, 220, 240),
        BorderSizePixel = 0,
        AnchorPoint = Vector2.new(0.5, 0.5),
        Position = UDim2.new(0.5, 0, 0.5, 0),
        Size = UDim2.new(0, 10, 0, 2),
        ZIndex = 7,
        Name = "MinBar",
        Parent = MinimizeBtn
    })
    Custom:Create("UICorner", { CornerRadius = UDim.new(1, 0), Parent = MinBar })

    MinimizeBtn.MouseEnter:Connect(function()
        MinimizeBtn.BackgroundColor3 = Color3.fromRGB(26, 36, 54)
        MinStroke.Color = Color3.fromRGB(0, 195, 255)
        MinBar.BackgroundColor3 = Color3.fromRGB(0, 195, 255)
    end)
    MinimizeBtn.MouseLeave:Connect(function()
        MinimizeBtn.BackgroundColor3 = Color3.fromRGB(18, 22, 32)
        MinStroke.Color = Color3.fromRGB(36, 46, 66)
        MinBar.BackgroundColor3 = Color3.fromRGB(205, 220, 240)
    end)

    MinimizeBtn.Activated:Connect(function()
        if MainCardData and MainCardData.SetFillTransparency then
            MainCardData.SetFillTransparency(1, 0.18)
            MainCardData.SetStrokeTransparency(1, 0.18)
        end
        TweenService:Create(DropShadowHolder, TweenInfo.new(0.18, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {Size = UDim2.new(0, 0, 0, 0)}):Play()
        task.wait(0.18)
        DropShadowHolder.Visible = false
        DropShadowHolder.Size = SizeUi
        Main.BackgroundTransparency = 1
        if MainCardData and MainCardData.SetFillTransparency then
            MainCardData.SetFillTransparency(0, 0)
            MainCardData.SetStrokeTransparency(0, 0)
        end
    end)

    -- 2. Resize / Maximize Button (↗)
    local ResizeToggleBtn, ResizeStroke = CreateWindowControlBtn(4, "ResizeToggleBtn")
    local ResizeLbl = Custom:Create("TextLabel", {
        Font = Enum.Font.GothamBold,
        Text = "\226\134\151", -- ↗
        TextColor3 = Color3.fromRGB(205, 220, 240),
        TextSize = 13,
        BackgroundTransparency = 1,
        Size = UDim2.new(1, 0, 1, 0),
        ZIndex = 7,
        Name = "ResizeLbl",
        Parent = ResizeToggleBtn
    })

    ResizeToggleBtn.MouseEnter:Connect(function()
        ResizeToggleBtn.BackgroundColor3 = Color3.fromRGB(26, 36, 54)
        ResizeStroke.Color = Color3.fromRGB(0, 195, 255)
        ResizeLbl.TextColor3 = Color3.fromRGB(0, 195, 255)
    end)
    ResizeToggleBtn.MouseLeave:Connect(function()
        ResizeToggleBtn.BackgroundColor3 = Color3.fromRGB(18, 22, 32)
        ResizeStroke.Color = Color3.fromRGB(36, 46, 66)
        ResizeLbl.TextColor3 = Color3.fromRGB(205, 220, 240)
    end)

    local isMaximized = false
    ResizeToggleBtn.Activated:Connect(function()
        isMaximized = not isMaximized
        local targetSize = isMaximized and UDim2.fromOffset(800, 500) or SizeUi
        TweenService:Create(DropShadowHolder, TweenInfo.new(0.25, Enum.EasingStyle.Quad), {Size = targetSize}):Play()
    end)

    -- 3. Close / Silang Button (X)
    local CloseBtn, CloseStroke = CreateWindowControlBtn(5, "CloseBtn")
    CloseBtn.BackgroundColor3 = Color3.fromRGB(22, 18, 28)
    CloseStroke.Color = Color3.fromRGB(48, 34, 46)

    local CloseLbl = Custom:Create("TextLabel", {
        Font = Enum.Font.GothamBold,
        Text = "X",
        TextColor3 = Color3.fromRGB(225, 230, 242),
        TextSize = 12,
        BackgroundTransparency = 1,
        Size = UDim2.new(1, 0, 1, 0),
        ZIndex = 7,
        Name = "CloseLbl",
        Parent = CloseBtn
    })

    CloseBtn.MouseEnter:Connect(function()
        CloseBtn.BackgroundColor3 = Color3.fromRGB(190, 25, 45)
        CloseStroke.Color = Color3.fromRGB(255, 45, 75)
        CloseLbl.TextColor3 = Color3.fromRGB(255, 255, 255)
    end)
    CloseBtn.MouseLeave:Connect(function()
        CloseBtn.BackgroundColor3 = Color3.fromRGB(22, 18, 28)
        CloseStroke.Color = Color3.fromRGB(48, 34, 46)
        CloseLbl.TextColor3 = Color3.fromRGB(225, 230, 242)
    end)

    CloseBtn.Activated:Connect(function()
        if MainCardData and MainCardData.SetFillTransparency then
            MainCardData.SetFillTransparency(1, 0.2)
            MainCardData.SetStrokeTransparency(1, 0.2)
        end
        TweenService:Create(DropShadowHolder, TweenInfo.new(0.2, Enum.EasingStyle.Quad), {Size = UDim2.new(0, 0, 0, 0)}):Play()
        task.wait(0.2)
        DropShadowHolder.Visible = false
        DropShadowHolder.Size = SizeUi
        Main.BackgroundTransparency = 1
        if MainCardData and MainCardData.SetFillTransparency then
            MainCardData.SetFillTransparency(0, 0)
            MainCardData.SetStrokeTransparency(0, 0)
        end
    end)

    Speed_Library.ActiveWindow = Tabs

    GlobalOpenClose.Activated:Connect(function()
        if DropShadowHolder then
            DropShadowHolder.Visible = not DropShadowHolder.Visible
            if DropShadowHolder.Visible then
                DropShadowHolder.Size = UDim2.fromOffset(BaseWidth * 0.9, BaseHeight * 0.9)
                TweenService:Create(DropShadowHolder, TweenInfo.new(0.25, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
                    Size = SizeUi
                }):Play()
                Main.BackgroundTransparency = 1
                if MainCardData and MainCardData.SetFillTransparency then
                    MainCardData.SetFillTransparency(0, 0)
                    MainCardData.SetStrokeTransparency(0, 0)
                end
            end
        end
    end)

    -- ─────────────────────────────────────────────────────────────
    -- NAVIGATION BAR (Search Box on Left + Horizontal Tabs on Right)
    -- ─────────────────────────────────────────────────────────────
    local NavRow = Custom:Create("Frame", {
        BackgroundColor3 = Color3.fromRGB(15, 14, 20),
        BackgroundTransparency = 1,
        BorderSizePixel = 0,
        Position = UDim2.new(0, 16, 0, 44),
        Size = UDim2.new(1, -32, 0, 36),
        ClipsDescendants = true,
        ZIndex = 3,
        Name = "NavRow"
    }, Main)

    -- Search Bar (ROG Cyber Dark with Chamfered Corners and Cyan Accent)
    local SearchBar, SearchBarData = Custom:CreateChamferCard({
        Parent = NavRow,
        Size = UDim2.new(0, 140, 0, 28),
        Position = UDim2.new(0, 0, 0.5, -14),
        Chamfer = 5,
        FillColor = Color3.fromRGB(12, 16, 24),
        StrokeColor = Color3.fromRGB(35, 48, 68),
        StrokeThickness = 1,
        ZIndex = 4,
        Name = "SearchBar"
    })

    Custom:Create("ImageLabel", {
        Image = "rbxassetid://7072720976", -- Search icon
        ImageColor3 = Color3.fromRGB(0, 195, 255),
        BackgroundTransparency = 1,
        Position = UDim2.new(0, 8, 0.5, -6),
        Size = UDim2.new(0, 12, 0, 12),
        ZIndex = 5
    }, SearchBar)

    local SearchInput = Custom:Create("TextBox", {
        Font = Enum.Font.Gotham,
        PlaceholderText = "Search...",
        PlaceholderColor3 = Color3.fromRGB(120, 115, 135),
        Text = "",
        TextColor3 = Color3.fromRGB(240, 240, 245),
        TextSize = 11,
        TextXAlignment = Enum.TextXAlignment.Left,
        BackgroundTransparency = 1,
        Position = UDim2.new(0, 26, 0, 0),
        Size = UDim2.new(1, -30, 1, 0),
        ClearTextOnFocus = false,
        ZIndex = 5,
        Name = "SearchInput"
    }, SearchBar)

    -- Tabs Scrolling Container
    local TabsContainer = Custom:Create("ScrollingFrame", {
        BackgroundTransparency = 1,
        BorderSizePixel = 0,
        Position = UDim2.new(0, 150, 0, 0),
        Size = UDim2.new(1, -150, 1, 0),
        CanvasSize = UDim2.new(0, 0, 0, 0),
        AutomaticCanvasSize = Enum.AutomaticSize.X,
        ScrollBarThickness = 0,
        ScrollingDirection = Enum.ScrollingDirection.X,
        ClipsDescendants = true,
        ZIndex = 4,
        Name = "TabsContainer"
    }, NavRow)

    TabsContainer.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseWheel then
            local scrollDelta = input.Position.Z
            local currentX = TabsContainer.CanvasPosition.X
            local maxX = math.max(0, TabsContainer.AbsoluteCanvasSize.X - TabsContainer.AbsoluteWindowSize.X)
            local newX = math.clamp(currentX - (scrollDelta * 60), 0, maxX)
            TweenService:Create(TabsContainer, TweenInfo.new(0.15, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                CanvasPosition = Vector2.new(newX, 0)
            }):Play()
        end
    end)

    local TabsLayout = Custom:Create("UIListLayout", {
        FillDirection = Enum.FillDirection.Horizontal,
        VerticalAlignment = Enum.VerticalAlignment.Center,
        Padding = UDim.new(0, 8),
        SortOrder = Enum.SortOrder.LayoutOrder
    }, TabsContainer)

    TabsLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
        TabsContainer.CanvasSize = UDim2.new(0, TabsLayout.AbsoluteContentSize.X + 20, 0, 0)
    end)


    -- ─────────────────────────────────────────────────────────────
    -- CONTENT AREA (2-Column Responsive Layout)
    -- ─────────────────────────────────────────────────────────────
    local ContentArea = Custom:Create("Frame", {
        BackgroundTransparency = 1,
        BorderSizePixel = 0,
        Position = UDim2.new(0, 16, 0, 84),
        Size = UDim2.new(1, -32, 1, -94),
        ClipsDescendants = true,
        ZIndex = 3,
        Name = "ContentArea"
    }, Main)

    -- ─────────────────────────────────────────────────────────────
    -- OVERLAY MODALS (Settings & Credits)
    -- ─────────────────────────────────────────────────────────────
    local ModalOverlay = Custom:Create("Frame", {
        BackgroundColor3 = Color3.fromRGB(8, 8, 12),
        BackgroundTransparency = 0.4,
        BorderSizePixel = 0,
        Position = UDim2.new(0, 0, 0, 0),
        Size = UDim2.new(1, 0, 1, 0),
        Visible = false,
        ZIndex = 20,
        Name = "ModalOverlay"
    }, Main)
    Custom:Create("UICorner", { CornerRadius = UDim.new(0, 12) }, ModalOverlay)

    local function CreatePopupCard(modalTitle)
        local popup, popupData = Custom:CreateChamferCard({
            Parent = ModalOverlay,
            Size = UDim2.new(0, 360, 0, 230),
            Position = UDim2.new(0.5, 0, 0.5, 0),
            AnchorPoint = Vector2.new(0.5, 0.5),
            Chamfer = 10,
            FillColor = Color3.fromRGB(16, 15, 22),
            StrokeColor = Color3.fromRGB(255, 45, 75),
            StrokeThickness = 1.2,
            ZIndex = 21,
            Name = modalTitle .. "Card"
        })

        local pTop = Custom:Create("Frame", {
            BackgroundTransparency = 1,
            Position = UDim2.new(0, 16, 0, 12),
            Size = UDim2.new(1, -32, 0, 24),
            ZIndex = 22
        }, popup)

        Custom:Create("TextLabel", {
            Font = Enum.Font.GothamBold,
            Text = modalTitle,
            TextColor3 = Color3.fromRGB(245, 245, 250),
            TextSize = 14,
            TextXAlignment = Enum.TextXAlignment.Left,
            BackgroundTransparency = 1,
            Size = UDim2.new(1, -30, 1, 0),
            ZIndex = 22,
            Parent = pTop
        })

        local closePBtn = Custom:Create("TextButton", {
            AnchorPoint = Vector2.new(1, 0.5),
            Position = UDim2.new(1, 0, 0.5, 0),
            Size = UDim2.new(0, 24, 0, 24),
            Text = "X",
            Font = Enum.Font.GothamBold,
            TextColor3 = Color3.fromRGB(220, 225, 235),
            TextSize = 12,
            BackgroundColor3 = Color3.fromRGB(22, 18, 28),
            BorderSizePixel = 0,
            ZIndex = 24,
            Name = "ClosePopupBtn",
            Parent = pTop
        })
        Custom:Create("UICorner", {
            CornerRadius = UDim.new(0, 4),
            Parent = closePBtn
        })
        local closePStroke = Custom:Create("UIStroke", {
            Color = Color3.fromRGB(48, 34, 46),
            Thickness = 1,
            ApplyStrokeMode = Enum.ApplyStrokeMode.Border,
            Parent = closePBtn
        })

        closePBtn.MouseEnter:Connect(function()
            closePBtn.BackgroundColor3 = Color3.fromRGB(190, 25, 45)
            closePStroke.Color = Color3.fromRGB(255, 45, 75)
            closePBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        end)
        closePBtn.MouseLeave:Connect(function()
            closePBtn.BackgroundColor3 = Color3.fromRGB(22, 18, 28)
            closePStroke.Color = Color3.fromRGB(48, 34, 46)
            closePBtn.TextColor3 = Color3.fromRGB(220, 225, 235)
        end)

        closePBtn.Activated:Connect(function()
            popup.Visible = false
            ModalOverlay.Visible = false
        end)

        local pBody = Custom:Create("Frame", {
            BackgroundTransparency = 1,
            Position = UDim2.new(0, 16, 0, 48),
            Size = UDim2.new(1, -32, 1, -60),
            ZIndex = 22
        }, popup)

        return pBody, popup
    end

    local SettingsBody, SettingsPopup = CreatePopupCard("Settings")
    local CreditsBody, CreditsPopup = CreatePopupCard("Credits")

    -- Setup Settings Body Content
    Custom:Create("TextLabel", {
        Font = Enum.Font.Gotham,
        Text = "Toggle Keybind: RightControl\nHub Theme: Shield Edition\nRendering: Optimized Luau\nPlatform: " .. (UserInputService.TouchEnabled and "Mobile" or "PC"),
        TextColor3 = Color3.fromRGB(180, 190, 210),
        TextSize = 12,
        TextXAlignment = Enum.TextXAlignment.Left,
        TextYAlignment = Enum.TextYAlignment.Top,
        BackgroundTransparency = 1,
        Size = UDim2.new(1, 0, 0, 90),
        ZIndex = 23,
        Parent = SettingsBody
    })

    local UnloadBtn = Custom:Create("TextButton", {
        BackgroundColor3 = Color3.fromRGB(26, 20, 38),
        BorderSizePixel = 0,
        Position = UDim2.new(0, 0, 1, -36),
        Size = UDim2.new(1, 0, 0, 32),
        Font = Enum.Font.GothamBold,
        Text = "Unload UI",
        TextColor3 = Color3.fromRGB(240, 100, 100),
        TextSize = 12,
        ZIndex = 24,
        Parent = SettingsBody
    })
    Custom:Create("UICorner", { CornerRadius = UDim.new(0, 6) }, UnloadBtn)
    Custom:Create("UIStroke", { Color = Color3.fromRGB(140, 40, 40), Thickness = 1 }, UnloadBtn)

    UnloadBtn.Activated:Connect(function()
        MainGui:Destroy()
        if GlobalOpenClose then GlobalOpenClose:Destroy() end
        Speed_Library.Unloaded = true
    end)

    -- Setup Credits Body Content
    Custom:Create("TextLabel", {
        Font = Enum.Font.Gotham,
        Text = "Project: Shield Edition\nDeveloper: ShielDTeam\nVersion: Freemium 2026\nSpecial thanks to all ShielD users!",
        TextColor3 = Color3.fromRGB(190, 200, 220),
        TextSize = 12,
        TextXAlignment = Enum.TextXAlignment.Left,
        TextYAlignment = Enum.TextYAlignment.Top,
        BackgroundTransparency = 1,
        Size = UDim2.new(1, 0, 1, 0),
        ZIndex = 23,
        Parent = CreditsBody
    })

    SettingsBtn.Activated:Connect(function()
        CreditsPopup.Visible = false
        SettingsPopup.Visible = true
        ModalOverlay.Visible = true
    end)

    CreditsBtn.Activated:Connect(function()
        SettingsPopup.Visible = false
        CreditsPopup.Visible = true
        ModalOverlay.Visible = true
    end)

    -- ─────────────────────────────────────────────────────────────
    -- TABS & SECTIONS ENGINE
    -- ─────────────────────────────────────────────────────────────
    local Tabs = {}
    local TabList = {}
    local ActiveTabObj = nil
    local TabCount = 0

    -- Live Search Engine
    local AllSearchableItems = {}

    SearchInput:GetPropertyChangedSignal("Text"):Connect(function()
        local query = string.lower(SearchInput.Text)
        for _, itemInfo in ipairs(AllSearchableItems) do
            if itemInfo.Frame and itemInfo.Frame.Parent then
                if query == "" then
                    itemInfo.Frame.Visible = true
                else
                    local match = string.find(string.lower(itemInfo.Title or ""), query, 1, true)
                    itemInfo.Frame.Visible = (match ~= nil)
                end
            end
        end
    end)

    function Tabs:CreateTab(TabConfig)
        TabConfig = TabConfig or {}
        local TabName = TabConfig[1] or TabConfig.Name or ("Tab " .. (TabCount + 1))
        local TabIcon = TabConfig[2] or TabConfig.Icon or ""
        TabCount = TabCount + 1

        -- Smart Icon Fallback
        if TabIcon == "" then
            local lower = string.lower(TabName)
            if string.find(lower, "main") or string.find(lower, "info") or string.find(lower, "general") then
                TabIcon = "rbxassetid://10723415903" -- ⓘ info
            elseif string.find(lower, "home") then
                TabIcon = "rbxassetid://10723356507" -- 🏠 home
            elseif string.find(lower, "farm") then
                TabIcon = "rbxassetid://10734975692" -- ⚔ swords
            elseif string.find(lower, "sea") or string.find(lower, "boat") or string.find(lower, "fish") then
                TabIcon = "rbxassetid://10734910680" -- ⛵ boat
            elseif string.find(lower, "player") or string.find(lower, "teleport") or string.find(lower, "pvp") then
                TabIcon = "rbxassetid://10747373176" -- 👥 players
            else
                TabIcon = "rbxassetid://10709770317" -- star/feature
            end
        end

        -- Tab Button in Navigation Bar (Calculated width, no layout conflicts)
        local textWidth = 36
        pcall(function()
            local bounds = TextService:GetTextSize(TabName, 13, Enum.Font.GothamBold, Vector2.new(1000, 36))
            textWidth = bounds.X
        end)
        local btnWidth = 16 + 6 + textWidth + 14

        local TabBtn = Custom:Create("TextButton", {
            AutoButtonColor = false,
            BackgroundColor3 = Color3.fromRGB(20, 18, 28),
            BackgroundTransparency = 1,
            Text = "",
            Size = UDim2.new(0, btnWidth, 1, 0),
            LayoutOrder = TabCount,
            ZIndex = 5,
            Name = "TabBtn_" .. TabName
        }, TabsContainer)

        local TabContent = Custom:Create("Frame", {
            BackgroundTransparency = 1,
            Size = UDim2.new(1, 0, 1, -4),
            Position = UDim2.new(0, 0, 0, 0),
            Name = "TabContent"
        }, TabBtn)

        local TabBtnLayout = Custom:Create("UIListLayout", {
            FillDirection = Enum.FillDirection.Horizontal,
            VerticalAlignment = Enum.VerticalAlignment.Center,
            HorizontalAlignment = Enum.HorizontalAlignment.Center,
            Padding = UDim.new(0, 6),
            SortOrder = Enum.SortOrder.LayoutOrder
        }, TabContent)

        local TabIconImg = Custom:Create("ImageLabel", {
            Image = TabIcon,
            ImageColor3 = Color3.fromRGB(140, 150, 175),
            BackgroundTransparency = 1,
            Size = UDim2.new(0, 16, 0, 16),
            Active = false,
            ZIndex = 6,
            LayoutOrder = 1,
            Name = "TabIcon"
        }, TabContent)

        local TabLabel = Custom:Create("TextLabel", {
            Font = Enum.Font.GothamBold,
            Text = TabName,
            TextColor3 = Color3.fromRGB(150, 160, 185),
            TextSize = 13,
            TextXAlignment = Enum.TextXAlignment.Left,
            BackgroundTransparency = 1,
            Size = UDim2.new(0, textWidth + 2, 0, 16),
            Active = false,
            ZIndex = 6,
            LayoutOrder = 2,
            Name = "TabLabel"
        }, TabContent)

        -- Underline indicator centered directly under this tab button
        local TabIndicatorGlow = Custom:Create("Frame", {
            BackgroundColor3 = Color3.fromRGB(255, 45, 75),
            BackgroundTransparency = 1,
            BorderSizePixel = 0,
            AnchorPoint = Vector2.new(0.5, 1),
            Position = UDim2.new(0.5, 0, 1, 0),
            Size = UDim2.new(1, 4, 0, 5),
            ZIndex = 5,
            Name = "TabIndicatorGlow"
        }, TabBtn)
        Custom:Create("UICorner", { CornerRadius = UDim.new(1, 0) }, TabIndicatorGlow)

        local TabIndicator = Custom:Create("Frame", {
            BackgroundColor3 = Color3.fromRGB(255, 45, 75),
            BackgroundTransparency = 1,
            BorderSizePixel = 0,
            AnchorPoint = Vector2.new(0.5, 1),
            Position = UDim2.new(0.5, 0, 1, -1),
            Size = UDim2.new(1, -4, 0, 2.5),
            ZIndex = 6,
            Name = "TabIndicator"
        }, TabBtn)
        Custom:Create("UICorner", { CornerRadius = UDim.new(1, 0) }, TabIndicator)

        TabBtn.MouseEnter:Connect(function()
            if ActiveTabObj ~= TabObj then
                TabIconImg.ImageColor3 = Color3.fromRGB(220, 230, 250)
                TabLabel.TextColor3 = Color3.fromRGB(240, 245, 255)
            end
        end)
        TabBtn.MouseLeave:Connect(function()
            if ActiveTabObj ~= TabObj then
                TabIconImg.ImageColor3 = Color3.fromRGB(140, 150, 175)
                TabLabel.TextColor3 = Color3.fromRGB(150, 160, 185)
            end
        end)

        -- 2-Column Content Views for this Tab
        local TabView = Custom:Create("Frame", {
            BackgroundTransparency = 1,
            BorderSizePixel = 0,
            Position = UDim2.new(0, 0, 0, 0),
            Size = UDim2.new(1, 0, 1, 0),
            ClipsDescendants = true,
            Visible = false,
            Name = "TabView_" .. TabName
        }, ContentArea)

        local LeftColumn = Custom:Create("ScrollingFrame", {
            BackgroundTransparency = 1,
            BorderSizePixel = 0,
            Position = UDim2.new(0, 0, 0, 0),
            Size = UDim2.new(0.5, -6, 1, 0),
            CanvasSize = UDim2.new(0, 0, 0, 0),
            ScrollBarThickness = 2,
            ScrollBarImageColor3 = Custom.AccentBright,
            ScrollBarImageTransparency = 0.6,
            ClipsDescendants = true,
            Name = "LeftColumn"
        }, TabView)

        local LeftLayout = Custom:Create("UIListLayout", {
            Padding = UDim.new(0, 10),
            SortOrder = Enum.SortOrder.LayoutOrder
        }, LeftColumn)

        LeftLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
            LeftColumn.CanvasSize = UDim2.new(0, 0, 0, LeftLayout.AbsoluteContentSize.Y + 20)
        end)

        local RightColumn = Custom:Create("ScrollingFrame", {
            BackgroundTransparency = 1,
            BorderSizePixel = 0,
            Position = UDim2.new(0.5, 6, 0, 0),
            Size = UDim2.new(0.5, -6, 1, 0),
            CanvasSize = UDim2.new(0, 0, 0, 0),
            ScrollBarThickness = 2,
            ScrollBarImageColor3 = Custom.AccentBright,
            ScrollBarImageTransparency = 0.6,
            ClipsDescendants = true,
            Name = "RightColumn"
        }, TabView)

        local RightLayout = Custom:Create("UIListLayout", {
            Padding = UDim.new(0, 10),
            SortOrder = Enum.SortOrder.LayoutOrder
        }, RightColumn)

        RightLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
            RightColumn.CanvasSize = UDim2.new(0, 0, 0, RightLayout.AbsoluteContentSize.Y + 20)
        end)

        -- Automatic Culling of corner accent lines when scrolled outside column visible viewport
        local function SetupColumnCulling(column)
            local function updateCulling()
                if not column.Visible or not column:IsDescendantOf(game) then return end
                local colTop = column.AbsolutePosition.Y
                local colBottom = colTop + column.AbsoluteSize.Y
                for _, secHolder in ipairs(column:GetChildren()) do
                    if secHolder:IsA("GuiObject") then
                        local itemsContainer = secHolder:FindFirstChild("ItemsContainer")
                        local list = itemsContainer and itemsContainer:GetChildren() or {secHolder}
                        for _, card in ipairs(list) do
                            if card:IsA("GuiObject") then
                                local cTop = card.AbsolutePosition.Y
                                local cBottom = cTop + card.AbsoluteSize.Y
                                local inView = (cBottom >= colTop - 4) and (cTop <= colBottom + 4)
                                local holder = card:FindFirstChild("CornerLinesHolder")
                                if holder then
                                    holder.Visible = inView
                                end
                                for _, sub in ipairs(card:GetChildren()) do
                                    if sub.Name == "CornerLinesHolder" and sub ~= holder then
                                        sub.Visible = inView
                                    end
                                end
                            end
                        end
                    end
                end
            end

            column:GetPropertyChangedSignal("CanvasPosition"):Connect(updateCulling)
            column:GetPropertyChangedSignal("AbsoluteSize"):Connect(updateCulling)
            return updateCulling
        end

        local updateLeftCulling = SetupColumnCulling(LeftColumn)
        local updateRightCulling = SetupColumnCulling(RightColumn)

        local TabObj = {
            Name = TabName,
            TabView = TabView,
            TabBtn = TabBtn,
            TabIndicator = TabIndicator,
            TabIndicatorGlow = TabIndicatorGlow,
            LeftColumn = LeftColumn,
            RightColumn = RightColumn,
            SectionCount = 0
        }

        local function ActivateTab()
            if ActiveTabObj == TabObj then return end

            -- Deactivate previous
            if ActiveTabObj then
                ActiveTabObj.TabView.Visible = false
                local prevContent = ActiveTabObj.TabBtn:FindFirstChild("TabContent")
                local prevIcon = (prevContent and prevContent:FindFirstChild("TabIcon")) or ActiveTabObj.TabBtn:FindFirstChild("TabIcon")
                local prevLbl = (prevContent and prevContent:FindFirstChild("TabLabel")) or ActiveTabObj.TabBtn:FindFirstChild("TabLabel")
                if prevIcon then prevIcon.ImageColor3 = Color3.fromRGB(140, 150, 175) end
                if prevLbl then prevLbl.TextColor3 = Color3.fromRGB(150, 160, 185) end
                if ActiveTabObj.TabIndicator then
                    TweenService:Create(ActiveTabObj.TabIndicator, TweenInfo.new(0.18, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                        BackgroundTransparency = 1,
                        Size = UDim2.new(0, 0, 0, 2.5)
                    }):Play()
                end
                if ActiveTabObj.TabIndicatorGlow then
                    TweenService:Create(ActiveTabObj.TabIndicatorGlow, TweenInfo.new(0.18, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                        BackgroundTransparency = 1
                    }):Play()
                end
            end

            -- Activate new
            ActiveTabObj = TabObj
            TabObj.TabView.Visible = true
            TabIconImg.ImageColor3 = Custom.AccentBright
            TabLabel.TextColor3 = Color3.fromRGB(255, 255, 255)

            -- Animate neon underline indicator directly under this tab
            TabIndicator.Size = UDim2.new(0, 0, 0, 2.5)
            TabIndicator.BackgroundTransparency = 0
            TabIndicatorGlow.BackgroundTransparency = 0.55
            TweenService:Create(TabIndicator, TweenInfo.new(0.25, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
                Size = UDim2.new(1, -4, 0, 2.5)
            }):Play()
            TweenService:Create(TabIndicatorGlow, TweenInfo.new(0.25, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                Size = UDim2.new(1, 4, 0, 5),
                BackgroundTransparency = 0.45
            }):Play()

            task.defer(function()
                pcall(updateLeftCulling)
                pcall(updateRightCulling)
            end)
        end

        TabBtn.Activated:Connect(ActivateTab)

        if TabCount == 1 then
            task.defer(ActivateTab)
        end

        -- ─────────────────────────────────────────────────────────
        -- SECTION CREATION (AddSection)
        -- ─────────────────────────────────────────────────────────
        function TabObj:AddSection(SectionTitle, OpenSection, Side, Locked)
            SectionTitle = SectionTitle or "Section"
            TabObj.SectionCount = TabObj.SectionCount + 1

            -- Auto-alternate columns if side is not explicitly specified
            if not Side or (Side ~= "Left" and Side ~= "Right") then
                Side = (TabObj.SectionCount % 2 == 1) and "Left" or "Right"
            end

            local TargetColumn = (Side == "Right") and RightColumn or LeftColumn

            local SectionHolder = Custom:Create("Frame", {
                BackgroundTransparency = 1,
                BorderSizePixel = 0,
                Size = UDim2.new(1, 0, 0, 0),
                AutomaticSize = Enum.AutomaticSize.Y,
                LayoutOrder = TabObj.SectionCount,
                Name = "Section_" .. SectionTitle
            }, TargetColumn)

            local SectionHolderLayout = Custom:Create("UIListLayout", {
                Padding = UDim.new(0, 8),
                SortOrder = Enum.SortOrder.LayoutOrder
            }, SectionHolder)

            -- ── Signature Quantum Onyx Section Header ──
            -- [ Left Pill Gradient ]  Title Text  [ Right Pill Gradient ]
            local HeaderFrame = Custom:Create("Frame", {
                BackgroundTransparency = 1,
                Size = UDim2.new(1, 0, 0, 24),
                LayoutOrder = 0,
                Name = "HeaderFrame"
            }, SectionHolder)

            local TitleLbl = Custom:Create("TextLabel", {
                Font = Enum.Font.GothamBold,
                Text = SectionTitle,
                TextColor3 = Color3.fromRGB(245, 245, 250),
                TextSize = 13,
                TextXAlignment = Enum.TextXAlignment.Center,
                BackgroundTransparency = 1,
                AnchorPoint = Vector2.new(0.5, 0.5),
                Position = UDim2.new(0.5, 0, 0.5, 0),
                Size = UDim2.new(0, 0, 1, 0),
                AutomaticSize = Enum.AutomaticSize.X,
                ZIndex = 4,
                Name = "TitleLbl"
            }, HeaderFrame)

            -- Left Pill: ROG Ruby Crimson Gradient
            local LeftPill = Custom:Create("Frame", {
                BackgroundColor3 = Color3.fromRGB(255, 45, 75),
                BorderSizePixel = 0,
                AnchorPoint = Vector2.new(1, 0.5),
                Position = UDim2.new(0.5, -45, 0.5, 0),
                Size = UDim2.new(0, 40, 0, 6),
                ZIndex = 4,
                Name = "LeftPill"
            }, HeaderFrame)
            Custom:Create("UICorner", { CornerRadius = UDim.new(1, 0) }, LeftPill)

            Custom:Create("UIGradient", {
                Color = ColorSequence.new{
                    ColorSequenceKeypoint.new(0, Color3.fromRGB(70, 10, 22)),
                    ColorSequenceKeypoint.new(1, Color3.fromRGB(255, 45, 75))
                }
            }, LeftPill)

            -- Right Pill: ROG Neon Red to Cyber Cyan Accent Gradient
            local RightPill = Custom:Create("Frame", {
                BackgroundColor3 = Color3.fromRGB(255, 45, 75),
                BorderSizePixel = 0,
                AnchorPoint = Vector2.new(0, 0.5),
                Position = UDim2.new(0.5, 45, 0.5, 0),
                Size = UDim2.new(0, 40, 0, 6),
                ZIndex = 4,
                Name = "RightPill"
            }, HeaderFrame)
            Custom:Create("UICorner", { CornerRadius = UDim.new(1, 0) }, RightPill)

            Custom:Create("UIGradient", {
                Color = ColorSequence.new{
                    ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 45, 75)),
                    ColorSequenceKeypoint.new(1, Color3.fromRGB(0, 195, 255))
                }
            }, RightPill)

            -- Keep pills snug against the title text width
            TitleLbl:GetPropertyChangedSignal("AbsoluteSize"):Connect(function()
                local halfW = TitleLbl.AbsoluteSize.X / 2
                LeftPill.Position = UDim2.new(0.5, -(halfW + 10), 0.5, 0)
                RightPill.Position = UDim2.new(0.5, (halfW + 10), 0.5, 0)
            end)

            -- Container for Section Items
            local ItemsContainer = Custom:Create("Frame", {
                BackgroundTransparency = 1,
                BorderSizePixel = 0,
                Size = UDim2.new(1, 0, 0, 0),
                AutomaticSize = Enum.AutomaticSize.Y,
                LayoutOrder = 1,
                Name = "ItemsContainer"
            }, SectionHolder)

            local ItemsLayout = Custom:Create("UIListLayout", {
                Padding = UDim.new(0, 7),
                SortOrder = Enum.SortOrder.LayoutOrder
            }, ItemsContainer)

            local Section = {
                Holder = SectionHolder,
                Container = ItemsContainer,
                ItemCount = 0,
                Locked = Locked or false
            }

            -- ─────────────────────────────────────────────────────
            -- COMPONENT 1: TOGGLE
            -- ─────────────────────────────────────────────────────
            function Section:AddToggle(TogConfig)
                TogConfig = TogConfig or {}
                local TogTitle = TogConfig[1] or TogConfig.Title or "Toggle"
                local TogContent = TogConfig[2] or TogConfig.Content or ""
                local Default = TogConfig[3] or TogConfig.Default or false
                local Callback = TogConfig[5] or TogConfig.Callback or function() end
                local IsLocked = TogConfig.Locked or TogConfig.Lock or false

                Section.ItemCount = Section.ItemCount + 1

                local Card, CardData = Custom:CreateComponentCard({
                    Parent = ItemsContainer,
                    Size = UDim2.new(1, 0, 0, 36),
                    LayoutOrder = Section.ItemCount,
                    CornerRadius = 6,
                    FillColor = Custom.CardDark,
                    StrokeColor = Custom.StrokeColor,
                    StrokeThickness = 1,
                    ZIndex = 2,
                    Name = "ToggleCard"
                })

                table.insert(AllSearchableItems, { Title = TogTitle, Frame = Card })

                -- Title
                Custom:Create("TextLabel", {
                    Font = Enum.Font.GothamBold,
                    Text = TogTitle,
                    TextColor3 = Color3.fromRGB(235, 235, 240),
                    TextSize = 12,
                    TextXAlignment = Enum.TextXAlignment.Left,
                    BackgroundTransparency = 1,
                    Position = UDim2.new(0, 12, 0, 0),
                    Size = UDim2.new(1, -65, 1, 0),
                    ZIndex = 4,
                    Parent = Card
                })

                -- Right Side Switch Container
                local SwitchHolder = Custom:Create("Frame", {
                    AnchorPoint = Vector2.new(1, 0.5),
                    BackgroundTransparency = 1,
                    Position = UDim2.new(1, -10, 0.5, 0),
                    Size = UDim2.new(0, 50, 0, 20),
                    ZIndex = 4,
                    Parent = Card
                })

                local SwitchLayout = Custom:Create("UIListLayout", {
                    FillDirection = Enum.FillDirection.Horizontal,
                    HorizontalAlignment = Enum.HorizontalAlignment.Right,
                    VerticalAlignment = Enum.VerticalAlignment.Center,
                    Padding = UDim.new(0, 6)
                }, SwitchHolder)

                -- Optional Lock Icon
                local LockIcon = Custom:Create("ImageLabel", {
                    Image = "rbxassetid://6031082533",
                    ImageColor3 = Color3.fromRGB(120, 115, 130),
                    BackgroundTransparency = 1,
                    Size = UDim2.new(0, 12, 0, 12),
                    Visible = IsLocked,
                    ZIndex = 5,
                    Parent = SwitchHolder
                })

                -- Pill Switch Frame
                local PillSwitch = Custom:Create("Frame", {
                    BackgroundColor3 = Default and Custom.AccentColor or Color3.fromRGB(26, 32, 46),
                    BorderSizePixel = 0,
                    Size = UDim2.new(0, 32, 0, 16),
                    ZIndex = 5,
                    Parent = SwitchHolder
                })
                Custom:Create("UICorner", { CornerRadius = UDim.new(1, 0) }, PillSwitch)

                local Knob = Custom:Create("Frame", {
                    AnchorPoint = Vector2.new(0, 0.5),
                    BackgroundColor3 = Default and Color3.fromRGB(255, 255, 255) or Color3.fromRGB(130, 125, 140),
                    BorderSizePixel = 0,
                    Position = Default and UDim2.new(1, -14, 0.5, 0) or UDim2.new(0, 2, 0.5, 0),
                    Size = UDim2.new(0, 12, 0, 12),
                    ZIndex = 6,
                    Parent = PillSwitch
                })
                Custom:Create("UICorner", { CornerRadius = UDim.new(1, 0) }, Knob)

                local ClickArea = Custom:Create("TextButton", {
                    Text = "",
                    BackgroundTransparency = 1,
                    Size = UDim2.new(1, 0, 1, 0),
                    ZIndex = 7,
                    Parent = Card
                })

                local TogObj = { Value = Default, Locked = IsLocked }

                local function UpdateState(val)
                    if TogObj.Locked then return end
                    TogObj.Value = val
                    local targetPos = val and UDim2.new(1, -14, 0.5, 0) or UDim2.new(0, 2, 0.5, 0)
                    local targetBg = val and Custom.AccentColor or Color3.fromRGB(26, 32, 46)
                    local targetKnob = val and Color3.fromRGB(255, 255, 255) or Color3.fromRGB(130, 125, 140)

                    TweenService:Create(PillSwitch, TweenInfo.new(0.15, Enum.EasingStyle.Quad), {BackgroundColor3 = targetBg}):Play()
                    TweenService:Create(Knob, TweenInfo.new(0.15, Enum.EasingStyle.Quad), {Position = targetPos, BackgroundColor3 = targetKnob}):Play()
                    pcall(Callback, val)
                    if TogObj.OnChangedFn then pcall(TogObj.OnChangedFn, val) end
                end

                ClickArea.MouseEnter:Connect(function()
                    CardData.SetFillColor(Color3.fromRGB(20, 26, 38), 0.15)
                    CardData.SetStrokeColor(Color3.fromRGB(50, 64, 88), 0.15)
                end)
                ClickArea.MouseLeave:Connect(function()
                    CardData.SetFillColor(Custom.CardDark, 0.15)
                    CardData.SetStrokeColor(Custom.StrokeColor, 0.15)
                end)

                ClickArea.Activated:Connect(function()
                    if not TogObj.Locked then
                        UpdateState(not TogObj.Value)
                    end
                end)

                function TogObj:Set(newVal)
                    UpdateState(newVal)
                end

                function TogObj:SetValue(newVal)
                    self:Set(newVal)
                end

                function TogObj:OnChanged(fn)
                    self.OnChangedFn = fn
                    return self
                end

                function TogObj:Lock()
                    self.Locked = true
                    LockIcon.Visible = true
                end

                function TogObj:Unlock()
                    self.Locked = false
                    LockIcon.Visible = false
                end

                if Default then
                    task.defer(function() pcall(Callback, true) end)
                end

                return TogObj
            end

            -- ─────────────────────────────────────────────────────
            -- COMPONENT 2: DROPDOWN
            -- ─────────────────────────────────────────────────────
            function Section:AddDropdown(DropConfig)
                DropConfig = DropConfig or {}
                local DropTitle = DropConfig[1] or DropConfig.Title or "Dropdown"
                local Multi = DropConfig[3] or DropConfig.Multi or false
                local Options = DropConfig[4] or DropConfig.Options or {}
                local Default = DropConfig[5] or DropConfig.Default or {}
                local Callback = DropConfig[6] or DropConfig.Callback or function() end
                local IsLocked = DropConfig.Locked or DropConfig.Lock or false

                if type(Default) ~= "table" then
                    Default = Default and {Default} or {}
                end

                Section.ItemCount = Section.ItemCount + 1

                local DropCard, DropCardData = Custom:CreateComponentCard({
                    Parent = ItemsContainer,
                    Size = UDim2.new(1, 0, 0, 36),
                    LayoutOrder = Section.ItemCount,
                    CornerRadius = 6,
                    FillColor = Custom.CardDark,
                    StrokeColor = Custom.StrokeColor,
                    StrokeThickness = 1,
                    ClipsDescendants = false,
                    ZIndex = 2,
                    Name = "DropdownCard"
                })

                table.insert(AllSearchableItems, { Title = DropTitle, Frame = DropCard })

                -- Top Row Header
                local TopRow = Custom:Create("Frame", {
                    BackgroundTransparency = 1,
                    Size = UDim2.new(1, 0, 0, 36),
                    ZIndex = 4,
                    Parent = DropCard
                })

                Custom:Create("TextLabel", {
                    Font = Enum.Font.GothamBold,
                    Text = DropTitle,
                    TextColor3 = Color3.fromRGB(235, 235, 240),
                    TextSize = 12,
                    TextXAlignment = Enum.TextXAlignment.Left,
                    BackgroundTransparency = 1,
                    Position = UDim2.new(0, 12, 0, 0),
                    Size = UDim2.new(1, -135, 1, 0),
                    ZIndex = 5,
                    Parent = TopRow
                })

                -- Pill Selector Box on Right [ Melee  v ] (Component Card Badge)
                local SelectorBox, SelectorData = Custom:CreateComponentCard({
                    Parent = TopRow,
                    Size = UDim2.new(0, 115, 0, 24),
                    Position = UDim2.new(1, -10, 0.5, 0),
                    AnchorPoint = Vector2.new(1, 0.5),
                    CornerRadius = 4,
                    FillColor = Color3.fromRGB(16, 20, 32),
                    StrokeColor = Color3.fromRGB(0, 195, 255),
                    StrokeThickness = 1,
                    ZIndex = 5,
                    Name = "SelectorBox"
                })

                local SelectedText = Custom:Create("TextLabel", {
                    Font = Enum.Font.GothamBold,
                    Text = (Default[1] and tostring(Default[1])) or "None",
                    TextColor3 = Color3.fromRGB(225, 240, 255),
                    TextSize = 11,
                    TextTruncate = Enum.TextTruncate.AtEnd,
                    TextXAlignment = Enum.TextXAlignment.Left,
                    BackgroundTransparency = 1,
                    Position = UDim2.new(0, 8, 0, 0),
                    Size = UDim2.new(1, -26, 1, 0),
                    ZIndex = 6,
                    Parent = SelectorBox
                })

                local Chevron = Custom:Create("TextLabel", {
                    Font = Enum.Font.GothamBold,
                    Text = "v",
                    TextColor3 = Color3.fromRGB(0, 195, 255),
                    TextSize = 9,
                    BackgroundTransparency = 1,
                    AnchorPoint = Vector2.new(1, 0.5),
                    Position = UDim2.new(1, -6, 0.5, 0),
                    Size = UDim2.new(0, 12, 0, 12),
                    ZIndex = 6,
                    Parent = SelectorBox
                })

                local DropdownToggleBtn = Custom:Create("TextButton", {
                    Text = "",
                    BackgroundTransparency = 1,
                    Size = UDim2.new(1, 0, 1, 0),
                    ZIndex = 7,
                    Parent = TopRow
                })

                -- Expandable Options List below Top Row (hidden by default when closed)
                local OptionsListFrame = Custom:Create("Frame", {
                    BackgroundTransparency = 1,
                    BorderSizePixel = 0,
                    Position = UDim2.new(0, 10, 0, 38),
                    Size = UDim2.new(1, -20, 0, 0),
                    Visible = false,
                    ClipsDescendants = true,
                    ZIndex = 5,
                    Parent = DropCard
                })

                -- Search Bar at the top of Dropdown List
                local DropSearchBox = Custom:Create("Frame", {
                    BackgroundColor3 = Color3.fromRGB(13, 17, 26),
                    BorderSizePixel = 0,
                    Position = UDim2.new(0, 0, 0, 0),
                    Size = UDim2.new(1, 0, 0, 24),
                    ZIndex = 6,
                    Parent = OptionsListFrame
                })

                Custom:Create("UICorner", {
                    CornerRadius = UDim.new(0, 4),
                    Parent = DropSearchBox
                })

                local DropSearchStroke = Custom:Create("UIStroke", {
                    Color = Color3.fromRGB(35, 48, 68),
                    Thickness = 1,
                    ApplyStrokeMode = Enum.ApplyStrokeMode.Border,
                    Parent = DropSearchBox
                })

                Custom:Create("ImageLabel", {
                    Image = "rbxassetid://7072720976", -- Search icon
                    ImageColor3 = Color3.fromRGB(0, 195, 255),
                    BackgroundTransparency = 1,
                    Position = UDim2.new(0, 7, 0.5, -5),
                    Size = UDim2.new(0, 10, 0, 10),
                    ZIndex = 7,
                    Parent = DropSearchBox
                })

                local DropSearchInput = Custom:Create("TextBox", {
                    Font = Enum.Font.Gotham,
                    PlaceholderText = "Search option...",
                    PlaceholderColor3 = Color3.fromRGB(110, 120, 135),
                    Text = "",
                    TextColor3 = Color3.fromRGB(240, 240, 245),
                    TextSize = 11,
                    TextXAlignment = Enum.TextXAlignment.Left,
                    BackgroundTransparency = 1,
                    Position = UDim2.new(0, 23, 0, 0),
                    Size = UDim2.new(1, -28, 1, 0),
                    ClearTextOnFocus = false,
                    ZIndex = 7,
                    Parent = DropSearchBox
                })

                DropSearchInput.Focused:Connect(function()
                    DropSearchStroke.Color = Color3.fromRGB(0, 195, 255)
                end)
                DropSearchInput.FocusLost:Connect(function()
                    DropSearchStroke.Color = Color3.fromRGB(35, 48, 68)
                end)

                -- Scrollable Options List below Search Bar
                local OptionsScroll = Custom:Create("ScrollingFrame", {
                    BackgroundTransparency = 1,
                    BorderSizePixel = 0,
                    Position = UDim2.new(0, 0, 0, 28),
                    Size = UDim2.new(1, 0, 0, 0),
                    CanvasSize = UDim2.new(0, 0, 0, 0),
                    AutomaticCanvasSize = Enum.AutomaticSize.Y,
                    ScrollBarThickness = 2,
                    ScrollBarImageColor3 = Color3.fromRGB(0, 195, 255),
                    ScrollBarImageTransparency = 0.35,
                    ClipsDescendants = true,
                    ZIndex = 6,
                    Parent = OptionsListFrame
                })

                local OptionsLayout = Custom:Create("UIListLayout", {
                    Padding = UDim.new(0, 4),
                    SortOrder = Enum.SortOrder.LayoutOrder
                }, OptionsScroll)

                local DropObj = {
                    Value = Default,
                    Options = Options,
                    IsOpen = false,
                    Locked = IsLocked
                }

                local function FormatSelected()
                    if Multi then
                        if #DropObj.Value == 0 then return "None" end
                        return table.concat(DropObj.Value, ", ")
                    else
                        return tostring(DropObj.Value[1] or "None")
                    end
                end

                local function CalcScrollDimensions(count)
                    count = count or 0
                    local itemH = 26
                    local itemPad = 4
                    local maxVisible = 4
                    local scrollH = 0
                    if count <= 0 then
                        scrollH = 24
                    elseif count <= maxVisible then
                        scrollH = (count * (itemH + itemPad)) - itemPad
                    else
                        scrollH = math.floor(maxVisible * (itemH + itemPad) + (itemH * 0.35))
                    end
                    local totalCardH = 38 + 28 + scrollH + 10
                    return scrollH, totalCardH
                end

                local function UpdateVisualHeights(animate)
                    if not DropObj.IsOpen then return end
                    local q = string.lower(DropSearchInput.Text)
                    local visibleCount = 0
                    for _, child in ipairs(OptionsScroll:GetChildren()) do
                        if child:IsA("Frame") and child.Name:sub(1, 4) == "Opt_" then
                            local optName = child:GetAttribute("OptionName") or ""
                            local show = (q == "") or (string.find(string.lower(optName), q, 1, true) ~= nil)
                            child.Visible = show
                            if show then
                                visibleCount = visibleCount + 1
                            end
                        end
                    end

                    local noResultLbl = OptionsScroll:FindFirstChild("NoResults")
                    if visibleCount == 0 and q ~= "" then
                        if not noResultLbl then
                            noResultLbl = Custom:Create("TextLabel", {
                                Font = Enum.Font.Gotham,
                                Text = "No options found",
                                TextColor3 = Color3.fromRGB(140, 140, 150),
                                TextSize = 11,
                                TextXAlignment = Enum.TextXAlignment.Center,
                                BackgroundTransparency = 1,
                                Size = UDim2.new(1, 0, 0, 24),
                                ZIndex = 7,
                                Name = "NoResults",
                                Parent = OptionsScroll
                            })
                        else
                            noResultLbl.Visible = true
                        end
                    elseif noResultLbl then
                        noResultLbl.Visible = false
                    end

                    local scrollH, totalCardH = CalcScrollDimensions(visibleCount)
                    OptionsScroll.Size = UDim2.new(1, 0, 0, scrollH)
                    OptionsListFrame.Size = UDim2.new(1, -20, 0, 28 + scrollH)

                    if animate then
                        TweenService:Create(DropCard, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                            Size = UDim2.new(1, 0, 0, totalCardH)
                        }):Play()
                    else
                        DropCard.Size = UDim2.new(1, 0, 0, totalCardH)
                    end
                end

                local function RebuildOptions()
                    for _, child in ipairs(OptionsScroll:GetChildren()) do
                        if child:IsA("Frame") and child.Name:sub(1, 4) == "Opt_" then
                            child:Destroy()
                        elseif child:IsA("TextLabel") and child.Name == "NoResults" then
                            child:Destroy()
                        end
                    end

                    for idx, opt in ipairs(DropObj.Options) do
                        local optStr = tostring(opt)
                        local isSelected = table.find(DropObj.Value, opt) ~= nil
                        local optBox = Custom:Create("Frame", {
                            BackgroundColor3 = isSelected and Color3.fromRGB(24, 32, 48) or Color3.fromRGB(16, 20, 30),
                            BorderSizePixel = 0,
                            Size = UDim2.new(1, -4, 0, 26),
                            ZIndex = 7,
                            LayoutOrder = idx,
                            Name = "Opt_" .. optStr,
                            Parent = OptionsScroll
                        })
                        optBox:SetAttribute("OptionName", optStr)

                        Custom:Create("UICorner", {
                            CornerRadius = UDim.new(0, 4),
                            Parent = optBox
                        })

                        local optStroke = Custom:Create("UIStroke", {
                            Color = isSelected and Color3.fromRGB(0, 195, 255) or Color3.fromRGB(28, 36, 50),
                            Thickness = 1,
                            ApplyStrokeMode = Enum.ApplyStrokeMode.Border,
                            Parent = optBox
                        })

                        local optLbl = Custom:Create("TextLabel", {
                            Font = Enum.Font.Gotham,
                            Text = "   " .. optStr,
                            TextColor3 = isSelected and Custom.AccentBright or Color3.fromRGB(200, 215, 235),
                            TextSize = 11,
                            TextXAlignment = Enum.TextXAlignment.Left,
                            BackgroundTransparency = 1,
                            Size = UDim2.new(1, 0, 1, 0),
                            ZIndex = 8,
                            Parent = optBox
                        })

                        local optBtn = Custom:Create("TextButton", {
                            Text = "",
                            BackgroundTransparency = 1,
                            Size = UDim2.new(1, 0, 1, 0),
                            ZIndex = 9,
                            Parent = optBox
                        })

                        optBtn.MouseEnter:Connect(function()
                            optBox.BackgroundColor3 = Color3.fromRGB(28, 38, 56)
                        end)
                        optBtn.MouseLeave:Connect(function()
                            local stillSelected = table.find(DropObj.Value, opt) ~= nil
                            optBox.BackgroundColor3 = stillSelected and Color3.fromRGB(24, 32, 48) or Color3.fromRGB(16, 20, 30)
                        end)

                        optBtn.Activated:Connect(function()
                            if DropObj.Locked then return end
                            if Multi then
                                local found = table.find(DropObj.Value, opt)
                                if found then
                                    table.remove(DropObj.Value, found)
                                else
                                    table.insert(DropObj.Value, opt)
                                end
                                SelectedText.Text = FormatSelected()
                                pcall(Callback, DropObj.Value)
                                RebuildOptions()
                                if DropObj.IsOpen then
                                    UpdateVisualHeights(false)
                                end
                            else
                                DropObj.Value = {opt}
                                SelectedText.Text = optStr
                                pcall(Callback, opt)
                                -- Auto close dropdown on single select
                                DropObj.IsOpen = false
                                Chevron.Text = "v"
                                DropSearchInput.Text = ""
                                local closeTw = TweenService:Create(DropCard, TweenInfo.new(0.18, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                                    Size = UDim2.new(1, 0, 0, 36)
                                })
                                closeTw:Play()
                                closeTw.Completed:Connect(function()
                                    if not DropObj.IsOpen then
                                        OptionsListFrame.Visible = false
                                        OptionsListFrame.Size = UDim2.new(1, -20, 0, 0)
                                    end
                                end)
                            end
                        end)
                    end
                end

                RebuildOptions()

                DropSearchInput:GetPropertyChangedSignal("Text"):Connect(function()
                    if DropObj.IsOpen then
                        UpdateVisualHeights(true)
                    end
                end)

                DropdownToggleBtn.Activated:Connect(function()
                    if DropObj.Locked then return end
                    DropObj.IsOpen = not DropObj.IsOpen
                    Chevron.Text = DropObj.IsOpen and "^" or "v"

                    if DropObj.IsOpen then
                        OptionsListFrame.Visible = true
                        DropSearchInput.Text = ""
                        UpdateVisualHeights(true)
                    else
                        local closeTw = TweenService:Create(DropCard, TweenInfo.new(0.18, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                            Size = UDim2.new(1, 0, 0, 36)
                        })
                        closeTw:Play()
                        closeTw.Completed:Connect(function()
                            if not DropObj.IsOpen then
                                OptionsListFrame.Visible = false
                                OptionsListFrame.Size = UDim2.new(1, -20, 0, 0)
                                DropSearchInput.Text = ""
                            end
                        end)
                    end
                end)

                function DropObj:Set(newVal)
                    if type(newVal) == "table" then
                        self.Value = newVal
                    else
                        self.Value = {newVal}
                    end
                    SelectedText.Text = FormatSelected()
                    RebuildOptions()
                    if self.IsOpen then
                        UpdateVisualHeights(false)
                    end
                    pcall(Callback, Multi and self.Value or (self.Value[1]))
                end

                function DropObj:SetValue(v)
                    self:Set(v)
                end

                function DropObj:Refresh(newOptions, newSelect)
                    self.Options = newOptions or {}
                    if newSelect then
                        self:Set(newSelect)
                    else
                        RebuildOptions()
                        if self.IsOpen then
                            UpdateVisualHeights(false)
                        end
                    end
                end

                function DropObj:Lock()
                    self.Locked = true
                end

                function DropObj:Unlock()
                    self.Locked = false
                end

                return DropObj
            end

            -- ─────────────────────────────────────────────────────
            -- COMPONENT 3: SLIDER
            -- ─────────────────────────────────────────────────────
            function Section:AddSlider(SliderConfig)
                SliderConfig = SliderConfig or {}
                local SldTitle = SliderConfig[1] or SliderConfig.Title or "Slider"
                local Increment = SliderConfig[3] or SliderConfig.Increment or 1
                local Min = SliderConfig[4] or SliderConfig.Min or 0
                local Max = SliderConfig[5] or SliderConfig.Max or 100
                local Default = SliderConfig[6] or SliderConfig.Default or Min
                local Callback = SliderConfig[7] or SliderConfig.Callback or function() end

                Section.ItemCount = Section.ItemCount + 1

                local SliderCard, SliderCardData = Custom:CreateComponentCard({
                    Parent = ItemsContainer,
                    Size = UDim2.new(1, 0, 0, 50),
                    LayoutOrder = Section.ItemCount,
                    CornerRadius = 6,
                    FillColor = Custom.CardDark,
                    StrokeColor = Custom.StrokeColor,
                    StrokeThickness = 1,
                    ZIndex = 2,
                    Name = "SliderCard"
                })

                table.insert(AllSearchableItems, { Title = SldTitle, Frame = SliderCard })

                -- Top row: Label Left, Value Badge Right
                local TopRow = Custom:Create("Frame", {
                    BackgroundTransparency = 1,
                    Position = UDim2.new(0, 12, 0, 8),
                    Size = UDim2.new(1, -24, 0, 16),
                    ZIndex = 4,
                    Parent = SliderCard
                })

                Custom:Create("TextLabel", {
                    Font = Enum.Font.GothamBold,
                    Text = SldTitle,
                    TextColor3 = Color3.fromRGB(235, 235, 240),
                    TextSize = 12,
                    TextXAlignment = Enum.TextXAlignment.Left,
                    BackgroundTransparency = 1,
                    Size = UDim2.new(1, -60, 1, 0),
                    ZIndex = 5,
                    Parent = TopRow
                })

                -- Corner Value Badge [ 250 ]
                local ValBadge, ValBadgeData = Custom:CreateComponentCard({
                    Parent = TopRow,
                    Size = UDim2.new(0, 48, 0, 18),
                    Position = UDim2.new(1, 0, 0.5, 0),
                    AnchorPoint = Vector2.new(1, 0.5),
                    CornerRadius = 3,
                    FillColor = Color3.fromRGB(20, 12, 18),
                    StrokeColor = Color3.fromRGB(255, 45, 75),
                    StrokeThickness = 1,
                    ZIndex = 5,
                    Name = "ValBadge"
                })

                local ValLabel = Custom:Create("TextLabel", {
                    Font = Enum.Font.GothamBold,
                    Text = tostring(Default),
                    TextColor3 = Custom.AccentGlow,
                    TextSize = 11,
                    BackgroundTransparency = 1,
                    Size = UDim2.new(1, 0, 1, 0),
                    ZIndex = 6,
                    Parent = ValBadge
                })

                -- Bottom row: Slider Track & Knob
                local TrackHolder = Custom:Create("Frame", {
                    BackgroundTransparency = 1,
                    Position = UDim2.new(0, 12, 0, 32),
                    Size = UDim2.new(1, -24, 0, 10),
                    ZIndex = 4,
                    Parent = SliderCard
                })

                local Track = Custom:Create("Frame", {
                    AnchorPoint = Vector2.new(0, 0.5),
                    BackgroundColor3 = Color3.fromRGB(22, 26, 36),
                    BorderSizePixel = 0,
                    Position = UDim2.new(0, 0, 0.5, 0),
                    Size = UDim2.new(1, 0, 0, 6),
                    ZIndex = 5,
                    Parent = TrackHolder
                })
                Custom:Create("UICorner", { CornerRadius = UDim.new(1, 0) }, Track)

                local Progress = Custom:Create("Frame", {
                    BackgroundColor3 = Custom.AccentBright,
                    BorderSizePixel = 0,
                    Size = UDim2.new(0, 0, 1, 0),
                    ZIndex = 6,
                    Parent = Track
                })
                Custom:Create("UICorner", { CornerRadius = UDim.new(1, 0) }, Progress)

                -- Circular Knob (White outer, ROG red inner)
                local Knob = Custom:Create("Frame", {
                    AnchorPoint = Vector2.new(0.5, 0.5),
                    BackgroundColor3 = Color3.fromRGB(255, 255, 255),
                    BorderSizePixel = 0,
                    Position = UDim2.new(0, 0, 0.5, 0),
                    Size = UDim2.new(0, 14, 0, 14),
                    ZIndex = 7,
                    Parent = Track
                })
                Custom:Create("UICorner", { CornerRadius = UDim.new(1, 0) }, Knob)

                local InnerDot = Custom:Create("Frame", {
                    AnchorPoint = Vector2.new(0.5, 0.5),
                    BackgroundColor3 = Custom.AccentColor,
                    BorderSizePixel = 0,
                    Position = UDim2.new(0.5, 0, 0.5, 0),
                    Size = UDim2.new(0, 6, 0, 6),
                    ZIndex = 8,
                    Parent = Knob
                })
                Custom:Create("UICorner", { CornerRadius = UDim.new(1, 0) }, InnerDot)

                local SliderObj = { Value = Default }

                local function UpdateFromVal(val)
                    val = math.clamp(val, Min, Max)
                    local percent = (val - Min) / (Max - Min)
                    Progress.Size = UDim2.new(percent, 0, 1, 0)
                    Knob.Position = UDim2.new(percent, 0, 0.5, 0)
                    ValLabel.Text = tostring(val)
                    SliderObj.Value = val
                    pcall(Callback, val)
                end

                local dragging = false
                local function MoveTo(inputX)
                    local trackAbs = Track.AbsolutePosition.X
                    local trackW = Track.AbsoluteSize.X
                    if trackW == 0 then return end
                    local percent = math.clamp((inputX - trackAbs) / trackW, 0, 1)
                    local rawVal = Min + (percent * (Max - Min))
                    local snapped = math.floor(rawVal / Increment + 0.5) * Increment
                    UpdateFromVal(snapped)
                end

                local moveConn, endConn
                TrackHolder.InputBegan:Connect(function(input)
                    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                        dragging = true
                        MoveTo(input.Position.X)

                        if moveConn then moveConn:Disconnect() moveConn = nil end
                        if endConn then endConn:Disconnect() endConn = nil end

                        moveConn = UserInputService.InputChanged:Connect(function(moveInput)
                            if dragging and (moveInput.UserInputType == Enum.UserInputType.MouseMovement or moveInput.UserInputType == Enum.UserInputType.Touch) then
                                MoveTo(moveInput.Position.X)
                            end
                        end)

                        endConn = UserInputService.InputEnded:Connect(function(endInput)
                            if endInput.UserInputType == Enum.UserInputType.MouseButton1 or endInput.UserInputType == Enum.UserInputType.Touch then
                                dragging = false
                                if moveConn then moveConn:Disconnect() moveConn = nil end
                                if endConn then endConn:Disconnect() endConn = nil end
                            end
                        end)
                    end
                end)

                function SliderObj:Set(v)
                    UpdateFromVal(v)
                end

                function SliderObj:SetValue(v)
                    self:Set(v)
                end

                UpdateFromVal(Default)
                return SliderObj
            end

            -- ─────────────────────────────────────────────────────
            -- COMPONENT 4: BUTTON
            -- ─────────────────────────────────────────────────────
            function Section:AddButton(BtnConfig)
                BtnConfig = BtnConfig or {}
                local BtnTitle = BtnConfig[1] or BtnConfig.Title or "Button"
                local Callback = BtnConfig[4] or BtnConfig.Callback or function() end

                Section.ItemCount = Section.ItemCount + 1

                local BtnCard, BtnCardData = Custom:CreateComponentCard({
                    Parent = ItemsContainer,
                    Size = UDim2.new(1, 0, 0, 36),
                    LayoutOrder = Section.ItemCount,
                    CornerRadius = 6,
                    FillColor = Custom.CardDark,
                    StrokeColor = Custom.StrokeColor,
                    StrokeThickness = 1,
                    ZIndex = 2,
                    Name = "ButtonCard"
                })

                table.insert(AllSearchableItems, { Title = BtnTitle, Frame = BtnCard })

                Custom:Create("TextLabel", {
                    Font = Enum.Font.GothamBold,
                    Text = BtnTitle,
                    TextColor3 = Color3.fromRGB(235, 235, 240),
                    TextSize = 12,
                    TextXAlignment = Enum.TextXAlignment.Left,
                    BackgroundTransparency = 1,
                    Position = UDim2.new(0, 12, 0, 0),
                    Size = UDim2.new(1, -24, 1, 0),
                    ZIndex = 4,
                    Parent = BtnCard
                })

                local ClickBtn = Custom:Create("TextButton", {
                    Text = "",
                    BackgroundTransparency = 1,
                    Size = UDim2.new(1, 0, 1, 0),
                    ZIndex = 5,
                    Parent = BtnCard
                })

                ClickBtn.MouseEnter:Connect(function()
                    BtnCardData.SetFillColor(Color3.fromRGB(22, 28, 42), 0.15)
                    BtnCardData.SetStrokeColor(Color3.fromRGB(55, 70, 98), 0.15)
                end)
                ClickBtn.MouseLeave:Connect(function()
                    BtnCardData.SetFillColor(Custom.CardDark, 0.15)
                    BtnCardData.SetStrokeColor(Custom.StrokeColor, 0.15)
                end)

                ClickBtn.Activated:Connect(function()
                    RippleClick(BtnCard, BtnCardData)
                    pcall(Callback)
                end)

                return {
                    Lock = function() end,
                    Unlock = function() end
                }
            end

            -- ─────────────────────────────────────────────────────
            -- COMPONENT 5: PARAGRAPH / INFORMATION CARD
            -- ─────────────────────────────────────────────────────
            function Section:AddParagraph(ParaConfig)
                ParaConfig = ParaConfig or {}
                local PTitle = ParaConfig[1] or ParaConfig.Title or ""
                local PContent = ParaConfig[2] or ParaConfig.Content or ""

                Section.ItemCount = Section.ItemCount + 1

                local textW = 270
                local contentH = 24
                pcall(function()
                    contentH = TextService:GetTextSize(PContent, 11, Enum.Font.Gotham, Vector2.new(textW, 10000)).Y
                end)
                local totalH = (PTitle ~= "" and (24 + contentH + 18) or (contentH + 20))
                totalH = math.max(totalH, 40)

                local Card, CardData = Custom:CreateComponentCard({
                    Parent = ItemsContainer,
                    Size = UDim2.new(1, 0, 0, totalH),
                    LayoutOrder = Section.ItemCount,
                    CornerRadius = 6,
                    FillColor = Custom.CardDark,
                    StrokeColor = Custom.StrokeColor,
                    StrokeThickness = 1,
                    ZIndex = 2,
                    Name = "ParagraphCard"
                })

                table.insert(AllSearchableItems, { Title = PTitle, Frame = Card })

                local yOffset = 10
                local TitleLabel
                if PTitle ~= "" then
                    TitleLabel = Custom:Create("TextLabel", {
                        Font = Enum.Font.GothamBold,
                        Text = PTitle,
                        TextColor3 = Color3.fromRGB(245, 245, 250),
                        TextSize = 13,
                        TextXAlignment = Enum.TextXAlignment.Left,
                        BackgroundTransparency = 1,
                        Position = UDim2.new(0, 12, 0, yOffset),
                        Size = UDim2.new(1, -24, 0, 16),
                        ZIndex = 4,
                        Parent = Card
                    })
                    yOffset = yOffset + 20
                end

                local ContentLabel = Custom:Create("TextLabel", {
                    Font = Enum.Font.Gotham,
                    Text = PContent,
                    TextColor3 = Color3.fromRGB(160, 170, 190),
                    TextSize = 11,
                    TextXAlignment = Enum.TextXAlignment.Left,
                    TextWrapped = true,
                    BackgroundTransparency = 1,
                    Position = UDim2.new(0, 12, 0, yOffset),
                    Size = UDim2.new(1, -24, 0, contentH),
                    ZIndex = 4,
                    Parent = Card
                })

                local ParaObj = {}
                function ParaObj:Set(newCfg)
                    if newCfg[1] or newCfg.Title then
                        PTitle = newCfg[1] or newCfg.Title
                        if TitleLabel then TitleLabel.Text = PTitle end
                    end
                    if newCfg[2] or newCfg.Content then
                        PContent = newCfg[2] or newCfg.Content
                        ContentLabel.Text = PContent
                    end
                    pcall(function()
                        contentH = TextService:GetTextSize(PContent, 11, Enum.Font.Gotham, Vector2.new(textW, 10000)).Y
                    end)
                    local newTotalH = (PTitle ~= "" and (24 + contentH + 18) or (contentH + 20))
                    newTotalH = math.max(newTotalH, 40)
                    ContentLabel.Size = UDim2.new(1, -24, 0, contentH)
                    TweenService:Create(Card, TweenInfo.new(0.15, Enum.EasingStyle.Quad), {Size = UDim2.new(1, 0, 0, newTotalH)}):Play()
                end

                function ParaObj:SetTitle(title)
                    self:Set({Title = title, Content = PContent})
                end

                function ParaObj:SetContent(content)
                    self:Set({Title = PTitle, Content = content})
                end

                function ParaObj:SetText(content)
                    self:Set({Title = PTitle, Content = content})
                end

                return ParaObj
            end

            -- ─────────────────────────────────────────────────────
            -- COMPONENT 6: SEPARATOR & LINE
            -- ─────────────────────────────────────────────────────
            function Section:AddSeperator(SepConfig)
                local STitle = SepConfig[1] or SepConfig.Title or ""
                Section.ItemCount = Section.ItemCount + 1

                local Sep = Custom:Create("Frame", {
                    BackgroundTransparency = 1,
                    Size = UDim2.new(1, 0, 0, 20),
                    LayoutOrder = Section.ItemCount,
                    Parent = ItemsContainer
                })

                Custom:Create("TextLabel", {
                    Font = Enum.Font.GothamBold,
                    Text = STitle,
                    TextColor3 = Custom.AccentBright,
                    TextSize = 11,
                    TextXAlignment = Enum.TextXAlignment.Left,
                    BackgroundTransparency = 1,
                    Position = UDim2.new(0, 6, 0, 0),
                    Size = UDim2.new(1, -12, 1, 0),
                    Parent = Sep
                })

                return { Set = function(_, cfg) end }
            end
            Section.AddSeparator = Section.AddSeperator

            function Section:AddLine()
                Section.ItemCount = Section.ItemCount + 1
                local Line = Custom:Create("Frame", {
                    BackgroundColor3 = Custom.StrokeColor,
                    BorderSizePixel = 0,
                    Size = UDim2.new(1, 0, 0, 1),
                    LayoutOrder = Section.ItemCount,
                    Parent = ItemsContainer
                })
                return {}
            end

            -- ─────────────────────────────────────────────────────
            -- COMPONENT 7: INPUT / TEXTBOX
            -- ─────────────────────────────────────────────────────
            function Section:AddInput(InpConfig)
                InpConfig = InpConfig or {}
                local InpTitle = InpConfig[1] or InpConfig.Title or "Input"
                local InpDefault = InpConfig[3] or InpConfig.Default or ""
                local Placeholder = InpConfig[4] or InpConfig.Placeholder or "Type here..."
                local Callback = InpConfig[5] or InpConfig.Callback or function() end

                Section.ItemCount = Section.ItemCount + 1

                local InpCard, InpCardData = Custom:CreateComponentCard({
                    Parent = ItemsContainer,
                    Size = UDim2.new(1, 0, 0, 36),
                    LayoutOrder = Section.ItemCount,
                    CornerRadius = 6,
                    FillColor = Custom.CardDark,
                    StrokeColor = Custom.StrokeColor,
                    StrokeThickness = 1,
                    ZIndex = 2,
                    Name = "InputCard"
                })

                table.insert(AllSearchableItems, { Title = InpTitle, Frame = InpCard })

                Custom:Create("TextLabel", {
                    Font = Enum.Font.GothamBold,
                    Text = InpTitle,
                    TextColor3 = Color3.fromRGB(235, 235, 240),
                    TextSize = 12,
                    TextXAlignment = Enum.TextXAlignment.Left,
                    BackgroundTransparency = 1,
                    Position = UDim2.new(0, 12, 0, 0),
                    Size = UDim2.new(0.5, 0, 1, 0),
                    ZIndex = 4,
                    Parent = InpCard
                })

                local BoxHolder, BoxHolderData = Custom:CreateComponentCard({
                    Parent = InpCard,
                    Size = UDim2.new(0, 120, 0, 24),
                    Position = UDim2.new(1, -10, 0.5, 0),
                    AnchorPoint = Vector2.new(1, 0.5),
                    CornerRadius = 4,
                    FillColor = Color3.fromRGB(16, 20, 32),
                    StrokeColor = Color3.fromRGB(35, 48, 68),
                    StrokeThickness = 1,
                    ZIndex = 5,
                    Name = "BoxHolder"
                })

                local TextBox = Custom:Create("TextBox", {
                    Font = Enum.Font.Gotham,
                    PlaceholderText = Placeholder,
                    PlaceholderColor3 = Color3.fromRGB(110, 105, 125),
                    Text = InpDefault,
                    TextColor3 = Color3.fromRGB(240, 240, 245),
                    TextSize = 11,
                    BackgroundTransparency = 1,
                    Position = UDim2.new(0, 8, 0, 0),
                    Size = UDim2.new(1, -16, 1, 0),
                    ClearTextOnFocus = false,
                    Parent = BoxHolder
                })

                TextBox.Focused:Connect(function()
                    BoxHolderData.SetStrokeColor(Color3.fromRGB(0, 195, 255), 0.15)
                    BoxHolderData.SetCornerColor(Color3.fromRGB(0, 195, 255), 0.15)
                end)
                TextBox.FocusLost:Connect(function()
                    BoxHolderData.SetStrokeColor(Color3.fromRGB(35, 48, 68), 0.15)
                    BoxHolderData.SetCornerColor(Custom.Cyan, 0.15)
                    pcall(Callback, TextBox.Text)
                end)

                local InpObj = {
                    Value = InpDefault,
                    Set = function(self, val)
                        TextBox.Text = tostring(val)
                        self.Value = val
                    end,
                    SetValue = function(self, val)
                        self:Set(val)
                    end,
                    SetText = function(self, val)
                        self:Set(val)
                    end
                }
                return InpObj
            end
            Section.AddTextBox = Section.AddInput

            -- ─────────────────────────────────────────────────────
            -- COMPONENT 8: KEYBIND
            -- ─────────────────────────────────────────────────────
            function Section:AddKeybind(KeyConfig)
                KeyConfig = KeyConfig or {}
                local KeyTitle = KeyConfig[1] or KeyConfig.Title or "Keybind"
                local KeyDefault = KeyConfig[2] or KeyConfig.Default or Enum.KeyCode.RightControl
                local Callback = KeyConfig[3] or KeyConfig.Callback or function() end

                Section.ItemCount = Section.ItemCount + 1

                local KeyCard, KeyCardData = Custom:CreateComponentCard({
                    Parent = ItemsContainer,
                    Size = UDim2.new(1, 0, 0, 36),
                    LayoutOrder = Section.ItemCount,
                    CornerRadius = 6,
                    FillColor = Custom.CardDark,
                    StrokeColor = Custom.StrokeColor,
                    StrokeThickness = 1,
                    ZIndex = 2,
                    Name = "KeyCard"
                })

                table.insert(AllSearchableItems, { Title = KeyTitle, Frame = KeyCard })

                Custom:Create("TextLabel", {
                    Font = Enum.Font.GothamBold,
                    Text = KeyTitle,
                    TextColor3 = Color3.fromRGB(235, 235, 240),
                    TextSize = 12,
                    TextXAlignment = Enum.TextXAlignment.Left,
                    BackgroundTransparency = 1,
                    Position = UDim2.new(0, 12, 0, 0),
                    Size = UDim2.new(0.5, 0, 1, 0),
                    ZIndex = 4,
                    Parent = KeyCard
                })

                local BindHolder, BindData = Custom:CreateComponentCard({
                    Parent = KeyCard,
                    Size = UDim2.new(0, 90, 0, 24),
                    Position = UDim2.new(1, -10, 0.5, 0),
                    AnchorPoint = Vector2.new(1, 0.5),
                    CornerRadius = 4,
                    FillColor = Color3.fromRGB(16, 20, 32),
                    StrokeColor = Color3.fromRGB(0, 195, 255),
                    StrokeThickness = 1,
                    ZIndex = 5,
                    Name = "BindHolder"
                })

                local BindBtn = Custom:Create("TextButton", {
                    BackgroundTransparency = 1,
                    Size = UDim2.new(1, 0, 1, 0),
                    Font = Enum.Font.GothamBold,
                    Text = KeyDefault.Name or "None",
                    TextColor3 = Custom.AccentGlow,
                    TextSize = 11,
                    ZIndex = 6,
                    Parent = BindHolder
                })

                local currentKey = KeyDefault
                local listening = false

                BindBtn.Activated:Connect(function()
                    if listening then return end
                    listening = true
                    BindBtn.Text = "..."
                    local conn
                    conn = UserInputService.InputBegan:Connect(function(input)
                        if input.UserInputType == Enum.UserInputType.Keyboard then
                            currentKey = input.KeyCode
                            BindBtn.Text = currentKey.Name
                            listening = false
                            conn:Disconnect()
                            pcall(Callback, currentKey)
                        end
                    end)
                end)

                UserInputService.InputBegan:Connect(function(input, gpe)
                    if not gpe and input.KeyCode == currentKey then
                        pcall(Callback, currentKey)
                    end
                end)

                return { Value = currentKey }
            end

            function Section:Lock()
                self.Locked = true
            end

            function Section:Unlock()
                self.Locked = false
            end

            return Section
        end

        return TabObj
    end

    -- Group Compatibility for older scripts
    function Tabs:CreateGroup(GroupConfig)
        local GroupObj = {}
        function GroupObj:CreateTab(SubTabConfig)
            return Tabs:CreateTab(SubTabConfig)
        end
        return GroupObj
    end

    -- Periodic Memory Cleanup for Anti-lag during long AFK
    task.spawn(function()
        while task.wait(180) do
            pcall(collectgarbage, "collect")
        end
    end)

    function Tabs:SetVisible(vis)
        DropShadowHolder.Visible = vis
        if vis then
            DropShadowHolder.Size = UDim2.fromOffset(BaseWidth * 0.9, BaseHeight * 0.9)
            TweenService:Create(DropShadowHolder, TweenInfo.new(0.25, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
                Size = SizeUi
            }):Play()
            Main.BackgroundTransparency = 1
            if MainCardData and MainCardData.SetFillTransparency then
                MainCardData.SetFillTransparency(0, 0)
                MainCardData.SetStrokeTransparency(0, 0)
            end
        end
    end

    function Tabs:Toggle()
        Tabs:SetVisible(not DropShadowHolder.Visible)
    end

    function Tabs:OpenROGLauncher(rogConfig)
        rogConfig = rogConfig or {}
        rogConfig.OnLaunch = rogConfig.OnLaunch or function()
            Tabs:SetVisible(true)
        end
        return Speed_Library:CreateROGLauncher(rogConfig)
    end
    Tabs.OpenShieldLauncher = Tabs.OpenROGLauncher

    return Tabs
end

-- ===============================================================
-- SHIELD ARMOURY CRATE - GAME SELECTOR / LAUNCHER SYSTEM
-- ===============================================================
function Speed_Library:CreateROGLauncher(Config)
    Config = Config or {}
    local Title = Config.Title or "Shield Armoury Crate"
    local OnLaunch = Config.OnLaunch or function() end
    local OnClose = Config.OnClose or function() end

    local Games = Config.Games or {
        {
            Name = "Blox Fruits",
            Description = "Auto Farm Level • Sea 1, 2, 3 • Fruit Sniper",
            Duration = "Duration record: 1 Min",
            Status = "Status: Active • Freemium",
            Icon = "rbxthumb://type=GameThumbnail&id=2753915549&w=768&h=432",
            Banner = "rbxthumb://type=GameThumbnail&id=2753915549&w=768&h=432",
            PlaceIds = { 2753915549, 4442272183, 7449423635 },
            Accent = Color3.fromRGB(255, 45, 75)
        },
        {
            Name = "Fisch",
            Description = "Auto Cast • Auto Shake • Auto Reel • Mythic Catch",
            Duration = "Duration record: Live",
            Status = "Status: Supported • v.AutoFish",
            Icon = "rbxthumb://type=GameThumbnail&id=16732694052&w=768&h=432",
            Banner = "rbxthumb://type=GameThumbnail&id=16732694052&w=768&h=432",
            PlaceIds = { 16732694052 },
            Accent = Color3.fromRGB(0, 195, 255)
        },
        {
            Name = "Grow a Garden",
            Description = "Auto Plant • Auto Harvest • Auto Buy Gears",
            Duration = "Duration record: 5 Min",
            Status = "Status: Ready • GardenBot",
            Icon = "rbxthumb://type=GameThumbnail&id=126884695634066&w=768&h=432",
            Banner = "rbxthumb://type=GameThumbnail&id=126884695634066&w=768&h=432",
            PlaceIds = { 126884695634066 },
            Accent = Color3.fromRGB(50, 220, 120)
        },
        {
            Name = "Universal Hub",
            Description = "ESP • Speed • Fly • God Mode • Server Tools",
            Duration = "Duration record: 12 Min",
            Status = "Status: Universal • All Games",
            Icon = "rbxassetid://10709770317",
            Banner = "rbxthumb://type=GameThumbnail&id=2753915549&w=768&h=432",
            PlaceIds = {},
            Accent = Color3.fromRGB(255, 45, 75)
        }
    }

    local activeIndex = 1
    if Config.AutoDetect ~= false then
        local currentPlace = game.PlaceId
        for idx, g in ipairs(Games) do
            if g.PlaceIds and table.find(g.PlaceIds, currentPlace) then
                activeIndex = idx
                break
            end
        end
    end

    local GuiParent = RunService:IsStudio() and Player.PlayerGui or (gethui and gethui() or game:GetService("CoreGui"))
    local ROGGui = Custom:Create("ScreenGui", {
        Name = "Shield_ArmouryCrate_Launcher",
        ZIndexBehavior = Enum.ZIndexBehavior.Sibling,
        ResetOnSpawn = false
    }, GuiParent)
    ProtectGui(ROGGui)

    local LauncherHolder = Custom:Create("Frame", {
        BackgroundTransparency = 1,
        Size = UDim2.fromOffset(780, 440),
        AnchorPoint = Vector2.new(0.5, 0.5),
        Position = UDim2.new(0.5, 0, 0.5, 0),
        Name = "LauncherHolder"
    }, ROGGui)

    local MainFrame, MainFrameData = Custom:CreateChamferCard({
        Parent = LauncherHolder,
        Size = UDim2.new(1, 0, 1, 0),
        Chamfer = 14,
        FillColor = Color3.fromRGB(8, 10, 14),
        StrokeColor = Color3.fromRGB(36, 42, 58),
        StrokeThickness = 1.2,
        ClipsDescendants = false,
        Name = "MainFrame"
    })



    local TopBevelLine = Custom:Create("Frame", {
        BackgroundColor3 = Color3.fromRGB(45, 50, 68),
        BorderSizePixel = 0,
        Position = UDim2.new(0.5, -150, 0, 0),
        Size = UDim2.new(0, 300, 0, 2),
        ZIndex = 3
    }, MainFrame)
    Custom:Create("UICorner", { CornerRadius = UDim.new(1, 0) }, TopBevelLine)

    local TopBar = Custom:Create("Frame", {
        BackgroundTransparency = 1,
        Position = UDim2.new(0, 16, 0, 10),
        Size = UDim2.new(1, -32, 0, 36),
        ZIndex = 4,
        Name = "TopBar"
    }, MainFrame)
    MakeDraggable(TopBar, LauncherHolder)

    local GridBtn = Custom:Create("Frame", {
        BackgroundTransparency = 1,
        Position = UDim2.new(0, 0, 0.5, -14),
        Size = UDim2.fromOffset(28, 28),
        ZIndex = 5
    }, TopBar)
    Custom:Create("ImageLabel", {
        Image = "rbxassetid://10709768641",
        ImageColor3 = Color3.fromRGB(220, 220, 230),
        BackgroundTransparency = 1,
        Position = UDim2.new(0.5, -9, 0.5, -9),
        Size = UDim2.fromOffset(18, 18),
        ZIndex = 6
    }, GridBtn)

    local NavTabsHolder = Custom:Create("Frame", {
        BackgroundTransparency = 1,
        AnchorPoint = Vector2.new(0.5, 0.5),
        Position = UDim2.new(0.5, 0, 0.5, 0),
        Size = UDim2.new(0, 480, 1, 0),
        ZIndex = 5,
        Name = "NavTabsHolder"
    }, TopBar)

    Custom:Create("UIListLayout", {
        FillDirection = Enum.FillDirection.Horizontal,
        HorizontalAlignment = Enum.HorizontalAlignment.Center,
        VerticalAlignment = Enum.VerticalAlignment.Center,
        Padding = UDim.new(0, 24)
    }, NavTabsHolder)

    local tabItems = {"Game Library", "Console", "Connect", "Featured", "My Profile"}
    for idx, tabText in ipairs(tabItems) do
        local isLibrary = (idx == 1)
        local tBtn = Custom:Create("TextButton", {
            AutoButtonColor = false,
            BackgroundTransparency = 1,
            Text = "",
            Size = UDim2.new(0, 0, 1, 0),
            AutomaticSize = Enum.AutomaticSize.X,
            ZIndex = 6
        }, NavTabsHolder)

        local tLbl = Custom:Create("TextLabel", {
            Font = Enum.Font.GothamBold,
            Text = tabText,
            TextColor3 = isLibrary and Color3.fromRGB(255, 65, 85) or Color3.fromRGB(160, 165, 180),
            TextSize = 13,
            BackgroundTransparency = 1,
            Size = UDim2.new(0, 0, 1, 0),
            AutomaticSize = Enum.AutomaticSize.X,
            ZIndex = 7
        }, tBtn)

        if isLibrary then
            -- Glowing text stroke
            Custom:Create("UIStroke", {
                Color = Color3.fromRGB(255, 30, 60),
                Thickness = 1,
                Transparency = 0.3
            }, tLbl)

            -- Glowing curved neon underline indicator matching media_1789062566246.png
            local RedGlow = Custom:Create("Frame", {
                BackgroundColor3 = Color3.fromRGB(255, 35, 60),
                BackgroundTransparency = 0.5,
                BorderSizePixel = 0,
                Position = UDim2.new(0, -10, 1, -4),
                Size = UDim2.new(1, 20, 0, 6),
                ZIndex = 7
            }, tBtn)
            Custom:Create("UICorner", { CornerRadius = UDim.new(1, 0) }, RedGlow)

            local RedLine = Custom:Create("Frame", {
                BackgroundColor3 = Color3.fromRGB(255, 45, 75),
                BorderSizePixel = 0,
                Position = UDim2.new(0, -6, 1, -2),
                Size = UDim2.new(1, 12, 0, 2.5),
                ZIndex = 8
            }, tBtn)
            Custom:Create("UICorner", { CornerRadius = UDim.new(1, 0) }, RedLine)
        end

        tBtn.MouseEnter:Connect(function()
            if not isLibrary then tLbl.TextColor3 = Color3.fromRGB(220, 225, 240) end
        end)
        tBtn.MouseLeave:Connect(function()
            if not isLibrary then tLbl.TextColor3 = Color3.fromRGB(150, 155, 170) end
        end)
    end

    local CloseBtn = Custom:Create("TextButton", {
        AutoButtonColor = false,
        BackgroundColor3 = Color3.fromRGB(20, 24, 34),
        Position = UDim2.new(1, -30, 0.5, -14),
        Size = UDim2.fromOffset(28, 28),
        Text = "X",
        Font = Enum.Font.GothamBold,
        TextColor3 = Color3.fromRGB(190, 195, 210),
        TextSize = 13,
        ZIndex = 8
    }, TopBar)
    Custom:Create("UICorner", { CornerRadius = UDim.new(0, 5) }, CloseBtn)
    local closeStroke = Custom:Create("UIStroke", {
        Color = Color3.fromRGB(45, 52, 70),
        Thickness = 1,
        ApplyStrokeMode = Enum.ApplyStrokeMode.Border,
        Parent = CloseBtn
    })
    CloseBtn.MouseEnter:Connect(function()
        CloseBtn.BackgroundColor3 = Color3.fromRGB(160, 20, 40)
        closeStroke.Color = Color3.fromRGB(255, 45, 75)
        CloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    end)
    CloseBtn.MouseLeave:Connect(function()
        CloseBtn.BackgroundColor3 = Color3.fromRGB(20, 24, 34)
        closeStroke.Color = Color3.fromRGB(45, 52, 70)
        CloseBtn.TextColor3 = Color3.fromRGB(190, 195, 210)
    end)

    -- Full Click/Skip Screen Overlay
    local SkipClickArea = Custom:Create("TextButton", {
        Text = "",
        BackgroundTransparency = 1,
        Size = UDim2.new(1, 0, 1, 0),
        ZIndex = 15,
        Parent = MainFrame
    })

    -- ─────────────────────────────────────────────────────────────
    -- GACHA HORIZONTAL REEL CONTAINER
    -- ─────────────────────────────────────────────────────────────
    local ReelContainer = Custom:Create("Frame", {
        BackgroundColor3 = Color3.fromRGB(10, 12, 17),
        BorderSizePixel = 0,
        Position = UDim2.new(0, 20, 0, 60),
        Size = UDim2.new(1, -40, 0, 250),
        ClipsDescendants = true,
        ZIndex = 4,
        Name = "ReelContainer"
    }, MainFrame)
    Custom:Create("UICorner", { CornerRadius = UDim.new(0, 8) }, ReelContainer)
    Custom:Create("UIStroke", {
        Color = Color3.fromRGB(30, 36, 50),
        Thickness = 1,
        ApplyStrokeMode = Enum.ApplyStrokeMode.Border,
        Parent = ReelContainer
    })

    -- Subtle horizontal glow lines on top & bottom of the reel
    local ReelLineTop = Custom:Create("Frame", {
        BackgroundColor3 = Color3.fromRGB(0, 195, 255),
        BackgroundTransparency = 0.65,
        BorderSizePixel = 0,
        Position = UDim2.new(0, 0, 0, 0),
        Size = UDim2.new(1, 0, 0, 1),
        ZIndex = 5,
        Parent = ReelContainer
    })
    local ReelLineBtm = Custom:Create("Frame", {
        BackgroundColor3 = Color3.fromRGB(255, 45, 75),
        BackgroundTransparency = 0.65,
        BorderSizePixel = 0,
        Position = UDim2.new(0, 1, 1, -1),
        Size = UDim2.new(1, 0, 0, 1),
        ZIndex = 5,
        Parent = ReelContainer
    })

    -- Center Reticle / Winning Slot Guide
    local CenterGlowBox = Custom:Create("Frame", {
        BackgroundColor3 = Color3.fromRGB(255, 255, 255),
        BackgroundTransparency = 0.88,
        BorderSizePixel = 0,
        AnchorPoint = Vector2.new(0.5, 0.5),
        Position = UDim2.new(0.5, 0, 0.5, 0),
        Size = UDim2.fromOffset(176, 216),
        ZIndex = 5,
        Name = "CenterGlowBox"
    }, ReelContainer)
    Custom:Create("UICorner", { CornerRadius = UDim.new(0, 8) }, CenterGlowBox)
    Custom:Create("UIGradient", {
        Color = ColorSequence.new({
            ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 45, 75)),
            ColorSequenceKeypoint.new(0.5, Color3.fromRGB(0, 195, 255)),
            ColorSequenceKeypoint.new(1, Color3.fromRGB(255, 45, 75))
        }),
        Rotation = 45
    }, CenterGlowBox)
    local centerStroke = Custom:Create("UIStroke", {
        Color = Color3.fromRGB(0, 195, 255),
        Thickness = 1.5,
        Transparency = 0.4,
        ApplyStrokeMode = Enum.ApplyStrokeMode.Border,
        Parent = CenterGlowBox
    })

    -- Top and Bottom Neon Indicator Markers
    local TopMarker = Custom:Create("Frame", {
        BackgroundColor3 = Color3.fromRGB(255, 45, 75),
        BorderSizePixel = 0,
        AnchorPoint = Vector2.new(0.5, 0),
        Position = UDim2.new(0.5, 0, 0, 3),
        Size = UDim2.fromOffset(26, 4),
        ZIndex = 12,
        Parent = ReelContainer
    })
    Custom:Create("UICorner", { CornerRadius = UDim.new(1, 0) }, TopMarker)

    local BtmMarker = Custom:Create("Frame", {
        BackgroundColor3 = Color3.fromRGB(255, 45, 75),
        BorderSizePixel = 0,
        AnchorPoint = Vector2.new(0.5, 1),
        Position = UDim2.new(0.5, 0, 1, -3),
        Size = UDim2.fromOffset(26, 4),
        ZIndex = 12,
        Parent = ReelContainer
    })
    Custom:Create("UICorner", { CornerRadius = UDim.new(1, 0) }, BtmMarker)

    -- Reel Moving Strip
    local ReelStrip = Custom:Create("Frame", {
        BackgroundTransparency = 1,
        AnchorPoint = Vector2.new(0, 0.5),
        Position = UDim2.new(0, 480, 0.5, 0),
        Size = UDim2.new(0, 3500, 1, 0),
        ZIndex = 6,
        Name = "ReelStrip"
    }, ReelContainer)

    local RouletteItems = {
        {
            Name = "Blox Fruits",
            Sub = "Auto Farm Sea 1-3",
            Tag = "POPULAR",
            Icon = "rbxthumb://type=GameThumbnail&id=2753915549&w=768&h=432",
            Banner = "rbxthumb://type=GameThumbnail&id=2753915549&w=768&h=432",
            Accent = Color3.fromRGB(255, 45, 75)
        },
        {
            Name = "Universal Hub",
            Sub = "ESP • Fly • Speed",
            Tag = "UNIVERSAL",
            Icon = "rbxassetid://10709770317",
            Banner = "rbxthumb://type=GameThumbnail&id=2753915549&w=768&h=432",
            Accent = Color3.fromRGB(255, 140, 0)
        },
        {
            Name = "Grow a Garden",
            Sub = "Auto Harvest Bot",
            Tag = "SIMULATOR",
            Icon = "rbxthumb://type=GameThumbnail&id=126884695634066&w=768&h=432",
            Banner = "rbxthumb://type=GameThumbnail&id=126884695634066&w=768&h=432",
            Accent = Color3.fromRGB(50, 220, 120)
        },
        {
            Name = "The Forge",
            Sub = "Weapon Crafter",
            Tag = "RPG",
            Icon = "rbxthumb://type=GameThumbnail&id=76558904092080&w=768&h=432",
            Banner = "rbxthumb://type=GameThumbnail&id=76558904092080&w=768&h=432",
            Accent = Color3.fromRGB(180, 70, 255)
        },
        {
            Name = "Pet Sim 99",
            Sub = "Auto Hatch • Farm",
            Tag = "SIMULATOR",
            Icon = "rbxthumb://type=GameThumbnail&id=8737899170&w=768&h=432",
            Banner = "rbxthumb://type=GameThumbnail&id=8737899170&w=768&h=432",
            Accent = Color3.fromRGB(255, 210, 40)
        },
        {
            Name = "Blox Fruits",
            Sub = "Bounty Hunter",
            Tag = "POPULAR",
            Icon = "rbxthumb://type=GameThumbnail&id=2753915549&w=768&h=432",
            Banner = "rbxthumb://type=GameThumbnail&id=2753915549&w=768&h=432",
            Accent = Color3.fromRGB(255, 45, 75)
        },
        {
            Name = "Universal Hub",
            Sub = "Server Tools",
            Tag = "UNIVERSAL",
            Icon = "rbxassetid://10709770317",
            Banner = "rbxthumb://type=GameThumbnail&id=16732694052&w=768&h=432",
            Accent = Color3.fromRGB(255, 140, 0)
        },
        {
            Name = "Blade Ball",
            Sub = "Auto Parry 0ms",
            Tag = "COMBAT",
            Icon = "rbxthumb://type=GameThumbnail&id=13772394625&w=768&h=432",
            Banner = "rbxthumb://type=GameThumbnail&id=13772394625&w=768&h=432",
            Accent = Color3.fromRGB(230, 50, 50)
        },
        {
            Name = "Grow a Garden",
            Sub = "Auto Buy Seeds",
            Tag = "SIMULATOR",
            Icon = "rbxthumb://type=GameThumbnail&id=126884695634066&w=768&h=432",
            Banner = "rbxthumb://type=GameThumbnail&id=126884695634066&w=768&h=432",
            Accent = Color3.fromRGB(50, 220, 120)
        },
        -- WINNING TARGET (INDEX 10)
        {
            Name = "Fisch",
            Sub = "Auto Cast • Mythic Catch",
            Tag = "VERIFIED",
            Icon = "rbxthumb://type=GameThumbnail&id=16732694052&w=768&h=432",
            Banner = "rbxthumb://type=GameThumbnail&id=16732694052&w=768&h=432",
            Accent = Color3.fromRGB(0, 195, 255),
            IsTarget = true
        },
        {
            Name = "Universal Hub",
            Sub = "Admin Scripts",
            Tag = "UNIVERSAL",
            Icon = "rbxassetid://10709770317",
            Banner = "rbxthumb://type=GameThumbnail&id=2753915549&w=768&h=432",
            Accent = Color3.fromRGB(255, 140, 0)
        },
        {
            Name = "Blox Fruits",
            Sub = "Fruit Sniper",
            Tag = "POPULAR",
            Icon = "rbxthumb://type=GameThumbnail&id=2753915549&w=768&h=432",
            Banner = "rbxthumb://type=GameThumbnail&id=2753915549&w=768&h=432",
            Accent = Color3.fromRGB(255, 45, 75)
        },
        {
            Name = "The Forge",
            Sub = "Ore Miner",
            Tag = "RPG",
            Icon = "rbxthumb://type=GameThumbnail&id=76558904092080&w=768&h=432",
            Banner = "rbxthumb://type=GameThumbnail&id=76558904092080&w=768&h=432",
            Accent = Color3.fromRGB(180, 70, 255)
        },
        {
            Name = "Pet Sim 99",
            Sub = "Diamonds Farm",
            Tag = "SIMULATOR",
            Icon = "rbxthumb://type=GameThumbnail&id=8737899170&w=768&h=432",
            Banner = "rbxthumb://type=GameThumbnail&id=8737899170&w=768&h=432",
            Accent = Color3.fromRGB(255, 210, 40)
        }
    }

    local cardWidth = 160
    local cardHeight = 200
    local cardGap = 16
    local stepX = cardWidth + cardGap
    local winningIndex = 10
    local TargetCardUI = nil

    for idx, item in ipairs(RouletteItems) do
        local centerPosX = (idx - 1) * stepX + (cardWidth / 2)
        local cardBox = Custom:Create("Frame", {
            BackgroundColor3 = Color3.fromRGB(13, 16, 23),
            BorderSizePixel = 0,
            AnchorPoint = Vector2.new(0.5, 0.5),
            Size = UDim2.fromOffset(cardWidth, cardHeight),
            Position = UDim2.new(0, centerPosX, 0.5, 0),
            ClipsDescendants = true,
            ZIndex = 7,
            Name = "RouletteCard_" .. item.Name,
            Parent = ReelStrip
        })
        Custom:Create("UICorner", { CornerRadius = UDim.new(0, 8) }, cardBox)
        local cardStroke = Custom:Create("UIStroke", {
            Color = item.IsTarget and Color3.fromRGB(0, 195, 255) or Color3.fromRGB(36, 44, 60),
            Thickness = item.IsTarget and 1.2 or 1,
            ApplyStrokeMode = Enum.ApplyStrokeMode.Border,
            Parent = cardBox
        })

        -- Official Game Artwork Banner (Top Section, Height 105)
        local banner = Custom:Create("ImageLabel", {
            Image = item.Banner or item.Icon,
            ImageColor3 = Color3.fromRGB(255, 255, 255),
            BackgroundTransparency = 1,
            Size = UDim2.new(1, 0, 0, 105),
            Position = UDim2.new(0, 0, 0, 0),
            ScaleType = Enum.ScaleType.Crop,
            ZIndex = 8,
            Parent = cardBox
        })

        -- Smooth dark gradient fade overlay blending into bottom card body
        local bannerFade = Custom:Create("Frame", {
            BackgroundColor3 = Color3.fromRGB(13, 16, 23),
            BackgroundTransparency = 0,
            BorderSizePixel = 0,
            Position = UDim2.new(0, 0, 0, 45),
            Size = UDim2.new(1, 0, 0, 60),
            ZIndex = 9,
            Parent = cardBox
        })
        Custom:Create("UIGradient", {
            Color = ColorSequence.new({
                ColorSequenceKeypoint.new(0, Color3.fromRGB(13, 16, 23)),
                ColorSequenceKeypoint.new(1, Color3.fromRGB(13, 16, 23))
            }),
            Transparency = NumberSequence.new({
                NumberSequenceKeypoint.new(0, 1),
                NumberSequenceKeypoint.new(0.5, 0.4),
                NumberSequenceKeypoint.new(1, 0)
            }),
            Rotation = 90
        }, bannerFade)

        -- Category Tag Pill (Top Right on Banner)
        local tagPill = Custom:Create("Frame", {
            BackgroundColor3 = Color3.fromRGB(8, 10, 14),
            BackgroundTransparency = 0.25,
            AnchorPoint = Vector2.new(1, 0),
            Position = UDim2.new(1, -8, 0, 8),
            Size = UDim2.fromOffset(66, 17),
            ZIndex = 11,
            Parent = cardBox
        })
        Custom:Create("UICorner", { CornerRadius = UDim.new(0, 4) }, tagPill)
        Custom:Create("UIStroke", {
            Color = item.Accent or Color3.fromRGB(0, 195, 255),
            Thickness = 1,
            Transparency = 0.4,
            ApplyStrokeMode = Enum.ApplyStrokeMode.Border,
            Parent = tagPill
        })
        Custom:Create("TextLabel", {
            Font = Enum.Font.GothamBold,
            Text = item.Tag or "FEATURED",
            TextColor3 = item.Accent or Color3.fromRGB(0, 195, 255),
            TextSize = 8.5,
            TextXAlignment = Enum.TextXAlignment.Center,
            BackgroundTransparency = 1,
            Size = UDim2.new(1, 0, 1, 0),
            ZIndex = 12,
            Parent = tagPill
        })

        -- Official Game Icon Square Badge
        local iconBadge = Custom:Create("Frame", {
            BackgroundColor3 = Color3.fromRGB(18, 23, 34),
            Position = UDim2.new(0, 10, 0, 78),
            Size = UDim2.fromOffset(30, 30),
            ClipsDescendants = true,
            ZIndex = 10,
            Parent = cardBox
        })
        Custom:Create("UICorner", { CornerRadius = UDim.new(0, 6) }, iconBadge)
        Custom:Create("UIStroke", {
            Color = item.Accent or Color3.fromRGB(0, 195, 255),
            Thickness = 1.2,
            ApplyStrokeMode = Enum.ApplyStrokeMode.Border,
            Parent = iconBadge
        })
        local cardIconImage = (item.Name ~= "Universal Hub" and (item.Banner or item.Icon)) or item.Icon or item.Banner
        Custom:Create("ImageLabel", {
            Image = cardIconImage,
            ImageColor3 = Color3.fromRGB(255, 255, 255),
            BackgroundTransparency = 1,
            Size = UDim2.new(1, 0, 1, 0),
            ScaleType = Enum.ScaleType.Crop,
            ZIndex = 11,
            Parent = iconBadge
        })

        -- Game Title
        local cardTitle = Custom:Create("TextLabel", {
            Font = Enum.Font.GothamBold,
            Text = item.Name,
            TextColor3 = Color3.fromRGB(255, 255, 255),
            TextSize = 13.5,
            TextXAlignment = Enum.TextXAlignment.Left,
            BackgroundTransparency = 1,
            Position = UDim2.new(0, 10, 0, 116),
            Size = UDim2.new(1, -20, 0, 18),
            ZIndex = 10,
            Parent = cardBox
        })

        -- Subtitle Description
        local cardSub = Custom:Create("TextLabel", {
            Font = Enum.Font.Gotham,
            Text = item.Sub,
            TextColor3 = item.IsTarget and Color3.fromRGB(0, 215, 255) or Color3.fromRGB(135, 145, 165),
            TextSize = 9.5,
            TextXAlignment = Enum.TextXAlignment.Left,
            BackgroundTransparency = 1,
            Position = UDim2.new(0, 10, 0, 138),
            Size = UDim2.new(1, -20, 0, 28),
            TextWrapped = true,
            ZIndex = 10,
            Parent = cardBox
        })

        -- Bottom Accent Bar (3px)
        local btmBar = Custom:Create("Frame", {
            BackgroundColor3 = item.Accent or Color3.fromRGB(0, 195, 255),
            BorderSizePixel = 0,
            Position = UDim2.new(0, 0, 1, -3),
            Size = UDim2.new(1, 0, 0, 3),
            ZIndex = 10,
            Parent = cardBox
        })

        if item.IsTarget then
            TargetCardUI = {
                Card = cardBox,
                Title = cardTitle,
                Sub = cardSub,
                BtmBar = btmBar,
                Stroke = cardBox:FindFirstChildOfClass("UIStroke")
            }
        end
    end

    -- ─────────────────────────────────────────────────────────────
    -- BOTTOM STATUS & GACHA RESULT DISPLAY
    -- ─────────────────────────────────────────────────────────────
    local StatusContainer = Custom:Create("Frame", {
        BackgroundTransparency = 1,
        Position = UDim2.new(0, 30, 1, -110),
        Size = UDim2.new(1, -60, 0, 95),
        ZIndex = 5,
        Name = "StatusContainer"
    }, MainFrame)

    local StatusTitle = Custom:Create("TextLabel", {
        Font = Enum.Font.GothamBold,
        Text = "SELECTING GAME PROFILE...",
        TextColor3 = Color3.fromRGB(0, 195, 255),
        TextSize = 14,
        TextXAlignment = Enum.TextXAlignment.Center,
        BackgroundTransparency = 1,
        Position = UDim2.new(0, 0, 0, 6),
        Size = UDim2.new(1, 0, 0, 20),
        ZIndex = 6,
        Parent = StatusContainer
    })

    local StatusSub = Custom:Create("TextLabel", {
        Font = Enum.Font.Gotham,
        Text = "Automated Game Selector • Standby",
        TextColor3 = Color3.fromRGB(150, 160, 180),
        TextSize = 11,
        TextXAlignment = Enum.TextXAlignment.Center,
        BackgroundTransparency = 1,
        Position = UDim2.new(0, 0, 0, 28),
        Size = UDim2.new(1, 0, 0, 16),
        ZIndex = 6,
        Parent = StatusContainer
    })

    local SkipHint = Custom:Create("TextLabel", {
        Font = Enum.Font.Gotham,
        Text = "Tap anywhere to skip animation",
        TextColor3 = Color3.fromRGB(90, 100, 120),
        TextSize = 9.5,
        TextXAlignment = Enum.TextXAlignment.Center,
        BackgroundTransparency = 1,
        Position = UDim2.new(0, 0, 1, -22),
        Size = UDim2.new(1, 0, 0, 14),
        ZIndex = 6,
        Parent = StatusContainer
    })

    -- Gacha Fade-out Blackout Overlay
    local FadeOverlay = Custom:Create("Frame", {
        BackgroundColor3 = Color3.fromRGB(6, 7, 10),
        BackgroundTransparency = 1,
        BorderSizePixel = 0,
        Size = UDim2.new(1, 0, 1, 0),
        ZIndex = 50,
        Parent = MainFrame
    })
    Custom:Create("UICorner", { CornerRadius = UDim.new(0, 14) }, FadeOverlay)

    local isLaunched = false
    local rollTween = nil

    local function TriggerFinishLaunch()
        if isLaunched then return end
        isLaunched = true

        if rollTween then
            rollTween:Cancel()
        end

        local fischGame = {
            Name = "Fisch",
            Description = "Auto Cast • Auto Shake • Auto Reel • Mythic Catch",
            Duration = "Duration record: Live",
            Status = "Status: Supported • v.AutoFish",
            Icon = "rbxthumb://type=GameThumbnail&id=16732694052&w=768&h=432",
            Banner = "rbxthumb://type=GameThumbnail&id=16732694052&w=768&h=432",
            PlaceIds = { 16732694052 },
            Accent = Color3.fromRGB(0, 195, 255)
        }

        -- Snap to target position
        local reelWidth = ReelContainer.AbsoluteSize.X > 0 and ReelContainer.AbsoluteSize.X or 740
        local targetCenter = (winningIndex - 1) * stepX + (cardWidth / 2)
        local targetX = (reelWidth / 2) - targetCenter
        ReelStrip.Position = UDim2.new(0, targetX, 0.5, 0)

        -- 1. GACHA VICTORY BURST!
        StatusTitle.Text = "PROFILE MATCHED: FISCH"
        StatusTitle.TextColor3 = Color3.fromRGB(255, 45, 75)
        StatusSub.Text = "LAUNCHING FISCH SHIELD EDITION IN 5s..."
        StatusSub.TextColor3 = Color3.fromRGB(0, 220, 255)

        -- Markers pulse
        TopMarker.BackgroundColor3 = Color3.fromRGB(0, 195, 255)
        BtmMarker.BackgroundColor3 = Color3.fromRGB(0, 195, 255)
        TweenService:Create(TopMarker, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out, -1, true), {
            BackgroundColor3 = Color3.fromRGB(255, 45, 75)
        }):Play()
        TweenService:Create(BtmMarker, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out, -1, true), {
            BackgroundColor3 = Color3.fromRGB(255, 45, 75)
        }):Play()

        -- CenterGlowBox expands and glows
        TweenService:Create(CenterGlowBox, TweenInfo.new(0.3, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
            Size = UDim2.fromOffset(188, 228),
            BackgroundTransparency = 0.55
        }):Play()
        centerStroke.Color = Color3.fromRGB(255, 45, 75)
        centerStroke.Transparency = 0

        -- TargetCard pop
        if TargetCardUI and TargetCardUI.Card then
            TweenService:Create(TargetCardUI.Card, TweenInfo.new(0.3, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
                Size = UDim2.fromOffset(168, 208)
            }):Play()
        end

        -- Flash beam sweep across the card
        local FlashSweep = Custom:Create("Frame", {
            BackgroundColor3 = Color3.fromRGB(255, 255, 255),
            BackgroundTransparency = 0.4,
            BorderSizePixel = 0,
            Rotation = 35,
            AnchorPoint = Vector2.new(0.5, 0.5),
            Position = UDim2.new(0.5, -120, 0.5, 0),
            Size = UDim2.fromOffset(24, 300),
            ZIndex = 25,
            Parent = ReelContainer
        })
        TweenService:Create(FlashSweep, TweenInfo.new(0.4, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
            Position = UDim2.new(0.5, 120, 0.5, 0),
            BackgroundTransparency = 1
        }):Play()

        -- 5 Seconds Wait Period after Selection with Live Countdown & Tap to Skip
        local skipWait = false
        local skipConn = SkipClickArea.Activated:Connect(function()
            skipWait = true
        end)

        for remaining = 5, 1, -1 do
            if skipWait then break end
            StatusSub.Text = string.format("LAUNCHING FISCH SHIELD EDITION IN %ds...", remaining)
            SkipHint.Text = string.format("Starting game in %ds • Tap anywhere to skip", remaining)
            local t = 0
            while t < 1 and not skipWait do
                task.wait(0.05)
                t = t + 0.05
            end
        end

        pcall(function() skipConn:Disconnect() end)
        StatusSub.Text = "LAUNCHING FISCH SHIELD EDITION..."
        SkipHint.Text = ""

        -- 2. GACHA SCREEN FADE OUT ("screen nya memudar kek lagi gacha versi chart rog")
        TweenService:Create(FadeOverlay, TweenInfo.new(0.45, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
            BackgroundTransparency = 0
        }):Play()
        TweenService:Create(LauncherHolder, TweenInfo.new(0.45, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
            Size = UDim2.fromOffset(820, 460),
            Position = UDim2.new(0.5, 0, 0.48, 0)
        }):Play()

        task.wait(0.48)
        ROGGui:Destroy()
        OnLaunch(fischGame)
    end

    -- Skip on click anywhere
    SkipClickArea.Activated:Connect(TriggerFinishLaunch)

    CloseBtn.Activated:Connect(function()
        if isLaunched then return end
        isLaunched = true
        if rollTween then rollTween:Cancel() end
        TweenService:Create(LauncherHolder, TweenInfo.new(0.25), {Size = UDim2.new(0, 0, 0, 0)}):Play()
        task.wait(0.25)
        ROGGui:Destroy()
        OnClose()
    end)

    -- ─────────────────────────────────────────────────────────────
    -- START AUTOMATIC GACHA ROULETTE ROLL ANIMATION
    -- ─────────────────────────────────────────────────────────────
    task.spawn(function()
        task.wait(0.15)
        local reelWidth = ReelContainer.AbsoluteSize.X > 0 and ReelContainer.AbsoluteSize.X or 740
        local targetCenter = (winningIndex - 1) * stepX + (cardWidth / 2)
        local finalTargetX = (reelWidth / 2) - targetCenter

        -- Initial position with Card 1 or 2 in view
        local startX = (reelWidth / 2) - ((1 - 1) * stepX + (cardWidth / 2)) + 120
        ReelStrip.Position = UDim2.new(0, startX, 0.5, 0)

        -- Smooth easing roll (starts fast, slows down realistically, lands on Fisch)
        rollTween = TweenService:Create(ReelStrip, TweenInfo.new(2.4, Enum.EasingStyle.Cubic, Enum.EasingDirection.Out), {
            Position = UDim2.new(0, finalTargetX, 0.5, 0)
        })
        rollTween:Play()

        rollTween.Completed:Connect(function()
            if not isLaunched then
                TriggerFinishLaunch()
            end
        end)
    end)

    local LauncherObj = {
        ScreenGui = ROGGui,
        Launch = TriggerFinishLaunch,
        Close = function()
            ROGGui:Destroy()
            OnClose()
        end
    }
    return LauncherObj
end

Speed_Library.CreateShieldLauncher = Speed_Library.CreateROGLauncher
Speed_Library.ThemeGameHud = function(self, cfg) return Custom:ApplyGameHudTheme(cfg) end
Speed_Library.ApplyGameHudTheme = function(self, cfg) return Custom:ApplyGameHudTheme(cfg) end

return Speed_Library
